# DECISIONS.md

작성 규칙은 [CONVENTION.md](./CONVENTION.md) 참고.

## 결정 기록

### 보드/컬럼/카드 책임 분리 설계
- 결정: 보드 단위에 컬럼을 포함한 컴포넌트로 카드를 담는 컨테이너 역할, 카드는 데이터만 보유한 형태로 최초 설계.
- 이유: 보드는 컬럼의 기능을 보유한 속성을 책임으로 갖고, 카드는 데이터만 관리하도록 분리하는 것으로 유지보수와 확장성 확보.
- 대안 및 기각 사유: 데이터와 로직을 철저히 분리한 설계를 할것인가? 현재는 프로젝트 규모 대비 과설계 및 아네믹 도메인 리스크가 있으므로 해당 대안은 보류.

### 지원자 데이터 임시 처리
- 결정: mock API 방식이 정해지기 전까지 `src/lib/candidates.ts`의 로컬 배열(`SAMPLE_CANDIDATES`)을 페이지에 직접 주입해 카드 렌더링을 확인한다.
- 이유: 카드 컴포넌트가 실제 데이터 형태(이름/직무/지원일/현재 단계)로 올바르게 표시되는지 화면에서 검증하려면 최소한의 데이터가 필요했고, mock API 방식이 아직 미정이라 임시로 정적 데이터를 사용했다.
- 대안 및 기각 사유: 카드 컴포넌트만 만들고 어디에도 연결하지 않는 방법도 검토했으나 렌더링 확인이 불가능해 기각. mock API 방식이 정해지면 `SAMPLE_CANDIDATES`는 실제 fetch 로직으로 교체될 예정.

