# DECISIONS.md

작성 규칙은 [CONVENTION.md](./CONVENTION.md) 참고.

## 결정 기록

### 보드/컬럼/카드 책임 분리 설계
- 결정: 보드 단위에 컬럼을 포함한 컴포넌트로 카드를 담는 컨테이너 역할, 카드는 데이터만 보유한 형태로 최초 설계.
- 이유: 보드는 컬럼의 기능을 보유한 속성을 책임으로 갖고, 카드는 데이터만 관리하도록 분리하는 것으로 유지보수와 확장성 확보.
- 대안 및 기각 사유: 데이터와 로직을 철저히 분리한 설계를 할것인가? 현재는 프로젝트 규모 대비 과설계 및 아네믹 도메인 리스크가 있으므로 해당 대안은 보류.

### 카드 상태 관리 및 낙관적 업데이트 롤백 전략
- 결정: 후보자 목록은 Board(클라이언트 컴포넌트)가 `useState`로 소유하는 단일 소스로 관리한다. 카드 이동 시 먼저 로컬 state를 낙관적으로 변경해 화면에 반영하고, mock API(PATCH) 호출 후 실패하면 이동 직전 단계로 되돌리고 에러 토스트로 사용자에게 알린다.
- 이유: 별도 상태 관리 라이브러리 없이도 보드 하나가 다루는 데이터 규모(단일 화면, 후보자 목록 하나)에서는 `useState` 로 충분하고, 이동 대상과 실패 시 되돌릴 이전 단계를 같은 클로저에서 다루는 편이 구현과 추적이 단순하다.
- 대안 및 기각 사유: Redux/Zustand 등 전역 상태 라이브러리 도입도 검토했으나 여러 화면/컴포넌트가 상태를 공유할 필요가 아직 없어 기각. React Query 같은 서버 상태 라이브러리의 낙관적 업데이트 기능도 고려했지만, mock API 규모상 직접 롤백 로직을 작성하는 편이 더 명확하다고 판단.

### Mock API 방식
- 결정: Next.js Route Handler(`src/app/api/candidates/[id]/route.ts`)로 PATCH를 받아 서버 메모리 저장소(`src/lib/server/candidate-store.ts`)의 단계 값을 갱신한다. 초기 데이터는 Server Component(`page.tsx`)가 저장소를 직접 읽어 내려준다.
- 이유: 실제 백엔드 없이도 진짜 네트워크 요청(fetch/PATCH)을 통해 저장하는 형태를 갖춰야 추후 지연·실패 시뮬레이션을 자연스럽게 얹을 수 있다. 저장소는 dev 모드(Turbopack)에서 API 라우트와 페이지가 서로 다른 모듈 인스턴스로 번들링되어 일반 모듈 스코프 변수로는 값이 공유되지 않는 문제가 있어, `globalThis`에 보관하는 방식으로 우회했다.
- 대안 및 기각 사유: 브라우저 `localStorage`에 직접 저장하는 방식도 검토했으나, "mock API에 저장"이라는 요구와 지연/실패 시뮬레이션을 붙이기 쉬운 형태를 우선해 서버 라우트 방식을 택했다. 다만 이 방식은 서버리스(예: Vercel) 배포 시 인스턴스마다 메모리가 분리되어 상태가 유실될 수 있어, 실제 배포 단계에서 재검토가 필요하다.

### 드래그앤드롭 구현 방식
- 결정: 별도 라이브러리 없이 HTML5 네이티브 드래그앤드롭(`draggable`, `dragstart`/`dragover`/`drop`)으로 구현한다.
- 이유: 컬럼 간 카드 이동이라는 단일 상호작용만 필요해 `@dnd-kit` 같은 라이브러리 없이도 충분히 구현 가능하고, 의존성을 늘리지 않을 수 있다.
- 대안 및 기각 사유: `@dnd-kit/core` 도입도 검토했으나(접근성·터치 지원이 더 좋음), 현재 범위(데스크톱 기준 컬럼 간 이동)에는 과한 선택이라 판단해 보류. 터치 기기 지원이 필요해지면 재검토.

