# DECISIONS.md

작성 규칙은 [CONVENTION.md](./CONVENTION.md) 참고.

## 결정 기록

### 보드/컬럼/카드 책임 분리 설계
- 결정: 보드 단위에 컬럼을 포함한 컴포넌트로 카드를 담는 컨테이너 역할, 카드는 데이터만 보유한 형태로 최초 설계.
- 이유: 보드는 컬럼의 기능을 보유한 속성을 책임으로 갖고, 카드는 데이터만 관리하도록 분리하는 것으로 유지보수와 확장성 확보.
- 대안 및 기각 사유: 데이터와 로직을 철저히 분리한 설계를 할것인가? 현재는 프로젝트 규모 대비 과설계 및 아네믹 도메인 리스크가 있으므로 해당 대안은 보류.

### 폴더 구조
- 결정: 컴포넌트는 `src/components/<도메인>/`(예: `src/components/board/`)에 두고, 단계 등 도메인 데이터/타입은 `src/lib/`에 둔다. 경로 별칭은 기존 `@/*` -> `src/*`를 그대로 사용한다.
- 이유: Next.js는 폴더 구조를 강제하지 않는다. 도메인별 components 폴더 + lib 폴더로 관심사를 분리하는 통상적인 방식을 택해, 이후 카드·mock API 등 기능이 늘어나도 같은 규칙으로 확장하기 쉽게 했다.
- 대안 및 기각 사유: `app/_components`처럼 라우트에 컴포넌트를 컬로케이션하는 방식도 검토했으나, 현재 라우트가 `/` 하나뿐이고 추후 다른 라우트에서 재사용할 가능성이 있어 루트 `src/components`를 선택했다.

