# DECISIONS.md

작성 규칙은 [CONVENTION.md](./CONVENTION.md) 참고.

## 결정 기록

## Next.js 선택
- 결정 : Next.js(App Router 및 Route Handler)를 사용해 개발
- 이유 : 프론트엔드와 Mock API를 하나의 프로젝트에서 함께 관리할 수 있고, Route Handler를 통해 카드 조회·이동·정렬 저장 API를 간단하게 구현 가능. 또한 네트워크 지연과 15% 실패율 같은 테스트 조건을 API 내부에서 직접 제어하기 용이. 요구사항은 새로고침 후에도 유지만 있으므로 반영구적 저장이 불필요하기에 Next Router와 싱글톤 구조로 쉽게 구현 가능.
- 대안 및 기각 사유:
  - 별도 Express 서버: 프론트엔드와 API 서버를 분리해야 하므로 프로젝트 설정과 실행 환경이 복잡.
  - json-server: 기본 CRUD 구현은 빠르지만, 카드 순서 계산·경쟁 상태 처리·지연 및 실패 시뮬레이션을 커스텀하기 위해 별도 설정 비용을 고려했을때 Next가 더 가벼움.
  - MSW: 요청 모킹과 테스트에는 적합하지만, 이번 과제에서는 새로고침 이후에도 상태를 유지하는 Mock API가 필요하므로 Next.js Route Handler 기반 자체 구현이 더 적합하다고 판단.

### 보드/컬럼/카드 책임 분리 설계
- 결정: 보드 단위에 컬럼을 포함한 컴포넌트로 카드를 담는 컨테이너 역할, 카드는 데이터만 보유한 형태로 최초 설계.
- 이유: 보드는 컬럼의 기능을 보유한 속성을 책임으로 갖고, 카드는 데이터만 관리하도록 분리하는 것으로 유지보수와 확장성 확보.
- 대안 및 기각 사유: 데이터와 로직을 철저히 분리한 설계를 할것인가? 현재는 프로젝트 규모 대비 과설계 및 아네믹 도메인 리스크가 있으므로 해당 대안은 보류.

### 입력 처리와 카드 이동 액션의 책임 분리
- 결정: 키보드 조작을 추가할 때, "입력을 해석하는 계층"과 "카드를 실제로 옮기고 저장하는 액션"을 분리해서 드래그앤드롭과 키보드가 동일한 이동 함수를 공유하도록 직접 지시
- 이유: 입력 방식(드래그, 키보드, 추후 다른 방식)이 늘어나도 이동/저장 로직은 하나만 유지하면 되고, 경쟁 상태 수정 같은 액션 쪽 변경이 입력 방식마다 중복 적용될 필요가 없도록 확장성을 확보
- 대안 및 기각 사유: 키보드 전용 이동 로직을 별도로 구현하는 방식은 드래그앤드롭과 로직이 중복되고, 두 경로의 동시성 처리를 각각 유지보수해야 해서 기각

### Undo 구현 방식
- 결정: 별도 서버 API/상태 없이, 클라이언트에 이동 히스토리를 스택(LIFO)으로 두고 기존 이동 액션을 재사용해 "이전 위치로 다시 이동"시키는 방식으로 구현. 이동이 실제로 성공한 경우에만 히스토리에 기록
- 이유: "mock API 에러 처리의 롤백과 꼬이지 않아야 한다"는 요구 때문에, 실패한 이동은 애초에 히스토리에 남기지 않아 낙관적 업데이트 롤백과 undo 스택이 서로 간섭하지 않도록 함
- 대안 및 기각 사유: 서버에 undo 전용 엔드포인트나 상태를 두는 방식 — mock API 범위를 벗어나는 과설계로 판단해 기각

### 동일 카드 연속 입력의 경쟁 상태 및 API 요청 처리
- 결정: 동일 카드의 이동 중 여부 판정은 React state가 아니라 동기적으로 확인할 수 있는 `ref` 기반 가드로 처리하고, 렌더링에 필요한 `movingIds` state는 별도로 유지한다. UI는 입력 즉시 낙관적으로 반영하되, 동일 카드의 연속 입력은 카드별 pending intent로 합친다. API 호출은 마지막 입력으로부터 500ms 동안 추가 입력이 없을 때 전송하며(trailing debounce), 요청이 진행 중인 동안 들어온 입력은 마지막 의도만 보관했다가 그 요청이 끝나는 즉시 이어서 전송한다. 카드당 동시에 1개 요청만 비행하도록 직렬화한다. 연속 입력 묶음의 시작 상태를 하나의 롤백 기준으로 삼으며, 최종 저장이 실패하면 해당 기준 상태로 복구하고 사용자에게 피드백한다.
- 이유: `setMovingIds`의 state updater 안에서 변경한 변수를 같은 함수 스코프에서 즉시 읽는 방식은 React의 실행 시점을 보장하지 못해, 빠른 키 입력에서 동일 카드 PATCH가 중복 발송될 수 있다. 동기적인 ref 가드로 요청 중복을 차단하고, 카드별 intent 병합으로 중간 상태에 대한 불필요한 API 호출과 확률적 실패 노출을 줄인다(5회 개별 전송 시 최소 1회 실패 확률 약 56%가 1회 전송 시 15%로 감소). 디바운스 대기 중 새로고침해 미저장되는 구간은 `pagehide` 시점의 `keepalive` 요청으로 보완한다.
- 대안 및 기각 사유:
  - 기존 `movingIds` state만 이용한 중복 차단: 상태 업데이터 실행 시점에 의존하므로 빠른 입력에서 가드가 통과할 수 있어 기각.
  - 모든 입력을 즉시 개별 API 요청으로 전송: 사용자 의도보다 요청 완료 순서가 최종 상태에 영향을 줄 수 있고, 네트워크 지연·15% 실패·정렬 기준점 경쟁을 불필요하게 확대하므로 기각.
  - 전역 디바운스 타이머 하나로 모든 카드의 요청을 병합: 서로 다른 카드의 입력이 서로의 타이머를 취소할 수 있으므로 기각. 디바운스와 pending intent는 카드별로 관리한다.
  - 서버에 모든 중간 이동을 순서대로 큐잉: 단일 탭 Mock API 범위에 비해 요청과 상태 관리가 복잡해지므로 우선 기각. 멀티탭·멀티세션까지 보장해야 할 경우에는 별도의 revision/version 검증을 추가 검토한다.

### 서버 응답을 화면에 반영하는 시점
- 결정: API 응답을 받으면 롤백 기준이 되는 "마지막 확정 위치"는 매번 갱신하되, 화면 갱신은 그 응답이 해당 연속 입력 묶음의 마지막 응답일 때(= 그 시점에 대기 중인 다음 의도가 없을 때)만 수행한다.
- 이유: 낙관적 업데이트의 계약은 "화면은 항상 최신 사용자 의도를 보여주고, 실패했을 때만 되돌린다"이다. 응답을 조건 없이 화면에 반영하면 화면에 남는 것이 "최신 사용자 의도"가 아니라 "가장 나중에 도착한 응답"이 되어, 네트워크 지연이 요청마다 다른 환경에서는 먼저 보낸 요청의 늦은 응답이 이미 반영된 최신 상태를 잠시 되돌린다. 실제로 3연속 이동 시 그 되돌아간 순간을 기준으로 다음 목적지가 계산되어, 동일한 목적지로 중복 요청이 나가고 마지막 이동이 반영되지 않는 현상을 계측으로 확인했다.
- 대안 및 기각 사유:
  - 낙관적 상태와 서버 확정 상태를 완전히 별도 자료구조로 분리: 더 견고하지만 이번 문제 범위에 비해 다뤄야 할 상태가 늘어나므로 기각. 화면 갱신 조건 하나로 동일한 결과를 얻을 수 있다고 판단.
  - 화면을 서버 응답과 영원히 분리(응답을 아예 반영하지 않음): 서버가 정렬 충돌 시 수행하는 재정렬 결과를 클라이언트가 따라잡지 못해 순서가 어긋난 채 남을 수 있어 기각. 묶음이 확정되는 마지막 시점에는 서버 값으로 동기화한다.

### Board 컴포넌트의 책임 분리 단위
- 결정: 한 파일에 몰려 있던 Board(467줄)를 데이터 수명주기·이동 엔진·화면으로 분리. 목록 조회와 보관은 `useCandidates`, 카드 이동의 낙관적 반영·디바운스 전송·실패 롤백·undo는 `useCardMoves`가 담당. 로딩/에러 분기는 데이터 수명주기 관련이므로 `Board`를 게이트로 남기고, 준비된 목록만 받는 `BoardView`와 공통 표시용 `BoardStatus`를 분리한다.
- 이유: 두 가지 근거. 첫째는 가독성으로, 한 파일 안에서 로딩·이동·undo·필터·렌더링이 섞여 있으면 특정 동작을 고칠 때 읽어야 할 범위가 파일 전체가 되어 비효율적. 둘째는 테스트 단위로, 버그가 집중된 이동·undo 로직이 렌더링과 붙어 있으면 검증할 때마다 브라우저를 띄워야 하는 불편 존재. 훅으로 떼어내면 `candidatesRef`와 `writeCandidates`만 넘겨 UI 없이 롤백·병합·직렬화를 직접 검증할 수 있고, `BoardView`도 fetch를 흉내내지 않고 데이터만으로 렌더링을 확인 가능.
- 대안 및 기각 사유:
  - undo를 별도 훅으로 추가 분리: 이동이 히스토리를 쌓고 undo가 다시 이동을 호출하는 순환 의존이라, 나누면 히스토리 push/pop이 두 파일로 흩어져 오히려 추적이 어려워지므로 기각. 이동 엔진과 같은 훅에 둔다.
  - `candidatesByStage`(필터 결과 + 스테이지 그룹핑)와 상세 패널 선택 상태까지 훅으로 분리: 전자는 조합 그 자체이고 후자는 3줄이라, 파일만 늘고 읽기는 더 어려워지므로 기각.

### 이동 중인 카드 주변으로 다른 카드를 옮길 때의 위치 경쟁 상태 보류
- 결정: 동일 카드의 연속 이동 직렬화는 현재 구현대로 보장하되, 아직 서버 확정 전인 카드를 정렬 기준점으로 삼는 경우의 상대 위치 오차와 동일 order 가능성은 알려진 제한으로 남긴다. 디바운스와 PATCH가 진행 중인 카드 주변에 다른 카드를 드롭하면 지정한 상대 위치가 보장되지 않는다(설정상 약 0.7~1.3초이며, 후속 이동이 이어지면 더 길어질 수 있다).
- 이유: X를 옮기는 중 Y를 X 바로 위에 드롭했을 때, 서버가 아직 이전 단계에 있는 X의 order를 기준으로 Y를 저장해 Y가 X 뒤쪽에 배치되고 지정한 순서가 뒤집히는 현상을 재현했다. 정확한 해결은 기준점이 확정될 때까지 후속 요청을 미루는 것으로 보이는데, 지금의 카드별 pending 구조에 단순 대기만 얹으면 A가 B를, B가 A를 기다리는 순환이 생긴다. 대안으로 전역 요청 순서를 검토했으나, 독립적인 카드까지 서버 확정 지연을 공유하게 되고 선행 이동이 실패했을 때 그것을 기준점으로 삼은 후속 이동을 어떻게 재배치할지 다시 정해야 한다. 어느 쪽이든 이동·롤백·undo 구조와 기존 테스트 전반을 함께 바꿔야 하며, 낙관적 업데이트라는 필수 요구 사항을 해칠 위험이 있다 판단했다.
- 대안 및 기각 사유:
  - 이동 중인 기준점을 주변의 확정된 카드로 치환: 두 이동이 같은 기준점 쌍을 쓰게 돼 서버 계산 결과까지 같아지는 것을 확인했다. 위치 오차가 동일 order 문제로 바뀔 뿐이라 기각.
  - 이동 중인 카드로의 드롭 자체를 거부: 잘못된 저장은 막지만, 저장 중 연속 드래그를 허용하려고 `f8fd80f`에서 제거한 드롭 대상 가드를 되살리는 셈이고 거부에 대한 별도 피드백도 필요해 기각.
  - 기준점 확정까지 후속 요청 대기 및 전역 순서 도입: 정확한 상대 위치를 보존할 수 있는 방향이지만, 변경 범위와 회귀 검증 비용 때문에 이번 범위에서는 보류.
