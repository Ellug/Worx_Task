# PROMPTS.md

작성 규칙은 [CONVENTION.md](./CONVENTION.md) 참고.

## [base] 문서 규칙 수립

### 프롬프트 1 (CLAUDE CODE)
```
지원자 채용 파이프라인 보드를 만들거야.
서류검토 - 면접 - 처우협의 - 최종합격 / 불합격 순이고, 지원자 카드를 단계간 이동하며 관리할거야
데이터가 제공되는 mock API(또는 로컬 mock 서버)를 통해 읽고 쓸 건데 실제 백엔드는 없어.

진행 방식은 기능 단위로 구현하고 커밋하는 방식이야. 요구사항을 기능 단위로 쪼개 점진적으로 커밋할거야. 커밋 히스토리를 남겨야해.
커밋 메시지 컨벤션은
feat(stage-move): 카드 단계 이동 + mock API 저장
- 드래그 대신 액션 버튼 방식 선택
- AI 초안은 로컬 상태만 갱신 -> API persist 누락, 직접 보완

type(scope): 요약 + 본문에 무엇을 왜
type은 feat/fix/refactor/test/docs/chore 형태로
커밋 메시지에 AI 초안을 어떻게 손봤는지 포함시키면 좋아

이걸 기본으로 일단 CONVENTION.md 를 만들어서 작업 기준으로 삼아

PROMPTS.md
프롬프트와 그에 대한 리뷰(검증)을 모두 md 파일로 남길거야. 기능 단위와 같은 순서로 정리해서 커밋과 기록이 1:1로 대응되게 정리할거야

## [stage-move] 카드 단계 이동

### 프롬프트 1
(AI에 준 지시 - 요약 아닌 실제 문구)

### AI 출력 요지
(무엇을 내놓았나)

### 리뷰 / 검증

- 무엇이 문제였나 (틀림, 비효율, 요구 누락)
- 어떻게 알아챘나 (직접 재현, 테스트, 정독)
- 그대로 채택 / 수정 / 기각 -> 어떤 판단을 왜 했나
- 수정했다면 어떻게 고쳤나

이런 형태로 고칠건데, 매 요청시 너가 갱신 가능한 부분은 갱신하고, 내가 직접 입력해야하는 부분은 간단 정리 혹은 공란으로 놔둬

DECISIONS.md 설계 결정 기록 문서도 관리할거야
상태 관리 방식, 낙관적 업데이트 롤백 전략, 폴더 구조, 라이브러리 선택 등 주요 결정 3~5개
AI 제안 중 채택/기각한 것과 이유. 못 끝낸 기능과 그 이유

설계 결정을 기록하는 곳이니 매 설계가 결정될 때마다 초안만 작성해
초안을 기반으로 내가 추가로 수정할테니

동작하는 코드 + README.md (설치 실행 명령 1~2줄, 선택한 스택 mockAPI방식 명시)
PROMPTS.md, DECISIONS.md 를 repo 최상단에 배치할거야
배포링크를 README에 추후에 추가할거야

우선 이걸 위한 베이스 작업을 진행할거야
기능은 아직 구현하지 말고 md문서와 규칙부터 기본적으로 수렴해
기능 구현은 내가 단위별로 명령할거야
```

### AI 출력 요지
- CONVENTION.md(커밋 컨벤션), PROMPTS.md(프롬프트-리뷰 기록 템플릿), DECISIONS.md(설계 결정 기록 틀) 신규 작성
- README.md를 create-next-app 기본 안내에서 프로젝트 소개 + 설치/실행 + 문서 링크로 교체
- `docs(base)` 커밋으로 반영

### 리뷰 / 검증
- PROMPTS.md에 "작성 규칙" 절을 그대로 넣어, 컨벤션(규칙) 성격의 내용이 기록용 문서에 섞임. 또한 사용자가 실제로 준 최초 지시 원문이 PROMPTS.md에 기록되지 않음
- 생성 된 문서 정독
- 작성 규칙은 CONVENTION.md 소관이 맞고, PROMPTS.md는 프롬프트-리뷰 기록 전용으로 유지하는 게 문서 역할 분리에 맞기에 수정 결정
- PROMPTS.md의 "작성 규칙" 절을 삭제하고 CONVENTION.md에 "PROMPTS.md 작성 규칙" 절로 이동, PROMPTS.md [base] 섹션에 최초 프롬프트 원문을 프롬프트 1로 추가


### 프롬프트 2 (CLAUDE CODE)
> PROMPTS.md에 내 최초 명령을 기재해
> 현재 PROMPTS.md에 정리돼있는 작성규칙은 컨벤션 md로 이동시켜

### AI 출력 요지
- PROMPTS.md의 "작성 규칙" 절을 삭제하고 CONVENTION.md에 "PROMPTS.md 작성 규칙" 절로 이동
- PROMPTS.md에 [base] 섹션을 추가하고, 최초 요구사항(프롬프트 1)과 이번 지시(프롬프트 2)를 실제 문구 그대로 기록

### 리뷰 / 검증
- 템플릿의 불필요한 문구 포함.
- 문서 정독.
- 템플릿은 가독성을 해치기 때문에 직접 수정

### 프롬프트 3 (CLAUDE CODE)
> 템플릿의 문구를 그대로 쓰지 마. 들어갈 내용의 지침이잖아. 내가 수정한 부분을 따라.
> PROMPTS와 DECISIONS는 최종 내용만 남아야 하기 때문에
> 템플릿을 포함한 안내 내용은 모두 CONVENTION.md로 이관해

### AI 출력 요지
- CONVENTION.md의 "PROMPTS.md 작성 규칙" / "DECISIONS.md 작성 규칙" 절에 각 문서의 목적 설명과 항목 템플릿을 옮기고, 템플릿은 그대로 옮겨 적는 문구가 아니라 지침이라는 점을 명시
- PROMPTS.md에서 안내 문단과 "항목 템플릿" 절을 삭제하고 CONVENTION.md 링크만 남김
- DECISIONS.md에서 안내 문단과 결정 항목 템플릿을 삭제하고 CONVENTION.md 링크만 남김, 프로젝트 고유 내용(다루어야 할 주요 결정 체크리스트 등)은 유지

### 리뷰 / 검증
- 기본적인 문서 컨벤션 형태 확인. 현상 유지.

### 프롬프트 4 (CODEX)
> 현재 디렉토리의 [CONVENTION.md](CONVENTION.md) [DECISIONS.md](DECISIONS.md) [PROMPTS.md](PROMPTS.md) 와 프로젝트 구조를 우선 파악해
> 콜르도와 병렬작업을 진행할 준비만 할거야. 코드의 작성은 하지말고 파악해서 메모리에만 올려서 작업 준비 상태만 만들어놔

### AI 출력 요지
- 코드 변경 없이 현재 구조 조회 완료

### 리뷰 / 검증
- 코드 변경 없음. 병렬 작업 준비 상태만 확인.

## [board-layout] 보드/컬럼 기본 구조

### 프롬프트 1 (CLAUDE CODE)
> 기능 구현을 시작할거야
> 지원자 카드를 단계별로 이동하기 위한 베이스인 보드와 컬럼을 메인 페이지상에 구현할거야.
> 컴포넌트 단위로 설계해서 적용해.
> 여기에 들어갈 카드가 후에 구현될 예정인 걸 고려해서 최초 구현 진행해

### AI 출력 요지
- `src/lib/stages.ts`: 단계 정의(서류검토/면접/처우협의/최종합격/불합격) + `Stage` 타입 신규 작성
- `src/components/board/Column.tsx`: 단계별 컬럼 컴포넌트. `children`(카드)이 없으면 빈 상태 문구를 보여주고, `children` 개수로 카운트 배지를 자동 계산해 추후 카드 컴포넌트를 children으로 넣기만 하면 되도록 설계
- `src/components/board/Board.tsx`: `STAGES`를 순회하며 `Column`을 가로 스크롤 레이아웃으로 렌더링
- `src/app/page.tsx`, `src/app/layout.tsx`: 메인 페이지에 Board 배치, 보드가 뷰포트를 채우도록 레이아웃/메타데이터 조정
- `npm run lint`·`tsc --noEmit` 통과 확인, dev 서버 기동 후 Playwright로 스크린샷 검증(5개 컬럼과 빈 상태 문구 정상 렌더링, 콘솔 에러 없음)

### 리뷰 / 검증
- 실제 화면에서 기본적인 UI 구조 정상 출력 확인
- 컴포넌트 책임 분리 의도대로 정상 적용 확인
- 그대로 채택

## [candidate-card] 지원자 카드 컴포넌트

### 프롬프트 1 (CLAUDE CODE)
> 카드 컴포넌트 작성을 진행해. 카드에는 이름, 직무, 지원일, 현재 단계가 표시되도록 작성해

### AI 출력 요지
- `src/lib/candidates.ts`: `Candidate` 타입(이름/직무/지원일/현재 단계) 신규 작성 + 화면 확인용 임시 샘플 데이터(`SAMPLE_CANDIDATES`) 6건 추가 (mock API 방식이 정해지기 전까지 로컬 데이터로만 사용)
- `src/components/board/CandidateCard.tsx`: 이름, 직무, 지원일, 현재 단계(색상 배지)를 표시하는 카드 컴포넌트 신규 작성
- `src/lib/stages.ts`: Column에 있던 단계별 색상 매핑을 `STAGE_DOT_TONE_CLASSNAME`/`STAGE_BADGE_TONE_CLASSNAME`으로 옮겨 Column과 CandidateCard가 함께 재사용하도록 정리
- `src/components/board/Board.tsx`: `candidates` prop을 받아 단계별로 필터링해 각 Column의 children으로 CandidateCard를 렌더링
- `src/app/page.tsx`: `SAMPLE_CANDIDATES`를 Board에 전달해 실제 카드가 보이도록 연결
- `npm run lint`·`tsc --noEmit` 통과 확인, dev 서버 기동 후 Playwright로 스크린샷 검증(카드별 이름/직무/지원일/단계 배지 정상 렌더링, 컬럼 카운트 배지 자동 반영 확인)

### 리뷰 / 검증
- 실제 화면에서 카드 UI 요구사항 표시 확인
- 카드 데이터 책임 분리 실제 코드상 확인
- 그대로 채택

## [stage-move] 카드 단계 이동 (드래그앤드롭 + mock API 저장 + 낙관적 업데이트)

### 프롬프트 1 (CLAUDE CODE)
> 카드를 드래그앤 드롭으로 단계를 이동할 수 있고, 이동한 데이터를 mock API에 저장해서 새로고침 후에도 유지하도록 해.
> 여기서 낙관적 업데이트 적용이 필요해. 이동시 UI를 먼저 반영하고, API 실패시 원상복구 + 사용자 피드백을 주도록 설정해.
> 지연 및 실패 시뮬레이션에 대한 건 개발 완료후에 진행할테니 기능 구현만 진행해

### AI 출력 요지
- Mock API: `src/app/api/candidates/[id]/route.ts` (PATCH)로 단계 변경을 저장, 상태는 `src/lib/server/candidate-store.ts`에 보관
- 드래그앤드롭: 네이티브 HTML5 DnD(draggable + dragstart/dragover/drop)로 구현, 별도 라이브러리 미도입
- 상태 관리: Board(client component)가 candidates 배열을 useState로 소유, page.tsx(Server Component)는 초기 데이터만 store에서 직접 읽어 전달
- 낙관적 업데이트: 드롭 즉시 로컬 state 변경 → PATCH 요청 → 실패 시 이동 전 단계로 롤백 + ErrorToast로 대상자 이름과 실패 사실 안내(자동 닫힘/수동 닫기 모두 지원)
- 이동 중인 카드는 draggable 비활성화 + "저장 중…" 표시로 같은 카드 중복 드래그 방지
- `npm run lint`·`tsc --noEmit` 통과 확인, 정상 이동 후 새로고침해도 유지되는지 검증 완료.

### 리뷰 / 검증
- 실제 화면에서 정상 작동 확인.
- 현재 md 문서에 지나치게 불필요한 정보를 클로드가 직접 작성하는 이슈가 확인 됨. 하지만 남은 시간을 고려해 이 이상 컨벤션을 수정하지 않고 우선 기능 개발 속행.

## [board-filter] 보드 필터 UI (이름 검색 + 직무 체크박스)

### 프롬프트 1 (CLAUDE CODE)
> 보드 위쪽에 필터 기능을 만들거야
> 우선 필터 UI를 구성해
> 컨테이너 컴포넌트 먼저 정의하고, 내부에 이름 검색 역할을 검색창과, 직무별 필터를 위한 체크박스를 구현할거야
>
> 인풋창, 체크박스에 대한 필터를 공통처리해야하니 로직을 외부로 빼는 게 좋다고 생각하는데
> 이런 구조가 데이터가 200~1000건 이상인 경우에 대응하기에 지연 없이 동작하도록 구성하기 적합한지 우선 판단하고, 적합한 구조면 진행해
> 부적합하다 판단하면 이유랑 근거, 대안을 나한테 제시하고

### AI 출력 요지
- 구조 판단: 필터 로직(이름 검색 + 직무 체크박스)을 컴포넌트 밖 커스텀 훅으로 분리하는 방향은 200~1000건 규모에 적합하다고 판단. 근거: 필터 predicate 자체가 O(n)이라 N=1000, 1000회 반복 벤치마크에서 1회당 평균 0.055ms로 연산 비용이 사실상 무시할 수준(node -e 벤치마크로 직접 측정), 진짜 병목은 필터링이 아니라 리렌더링이라 `useMemo`로 파생 리스트를 감싸고 검색어에 `useDeferredValue`를 적용하면 됨. 가상화는 이번 규모에선 불필요.
- `src/hooks/useCandidateFilter.ts`: candidates 배열을 받아 nameQuery/selectedPositions state, 직무 목록(allPositions), 파생된 filteredCandidates(useMemo)를 반환하는 훅으로 필터 로직을 UI와 분리
- `src/components/board/FilterBar.tsx`: 이름 검색 input(`type="search"`)과 직무별 체크박스(`fieldset`)를 담은 컨테이너 컴포넌트, 상태는 상위(Board)에서 controlled로 주입
- `src/components/board/Board.tsx`: useCandidateFilter를 사용해 FilterBar를 보드 상단에 배치하고, 컬럼에는 filteredCandidates만 전달 (카운트 배지도 필터 결과에 자동 반영)
- `npm run lint`·`tsc --noEmit` 통과 확인, Playwright로 이름 검색(6건→1건)과 직무 체크박스(6건→2건, 대상자 이름까지) 필터링이 정확히 동작하는지 직접 검증

### 리뷰 / 검증
- 실제 화면에서 필터 기능 정상 작동 확인
- 데이터가 많은 상태에서의 검증은 기능완성 이후 추후 예정
- 코드의 책임분리 의도대로 적용 확인. 채택.

## [candidate-detail] 지원자 상세 정보 사이드 패널

### 프롬프트 1 (CLAUDE CODE)
> 다음으로 각 카드 클릭시, 지원자 상세 정보를 읽을 수 있는 우측 사이드 패널을 제작해
> 패널 외부 클릭, esc키, 패널 우상단의 X 버튼을 통해서 패널을 닫을 수 있도록 해
> 패널이 열린 상태에서 다른 카드 조작을 방지하기 위해 패널로 방지하고

### AI 출력 요지
- `Candidate` 타입에 상세 정보 필드(email, phone, note) 추가, 시드 데이터 6건에 값 채움 (카드에는 그대로 이름/직무/지원일/단계만 노출)
- `src/components/board/CandidateDetailPanel.tsx`: 배경 백드롭 + 우측 슬라이드 패널을 함께 렌더링하는 컴포넌트. 백드롭은 전체 화면을 덮어 패널이 열린 동안 보드의 다른 카드 클릭/드래그를 차단하는 동시에, 백드롭 클릭 자체가 닫기 동작(패널 외부 클릭으로 닫기)이 되도록 구성. Esc 키다운 리스너와 X 버튼도 각각 닫기 호출
- `src/components/board/CandidateCard.tsx`: onClick으로 카드 클릭 시 상세 패널이 열리도록 연결
- `src/components/board/Board.tsx`: selectedCandidateId 상태로 선택된 지원자를 관리하고 패널을 렌더링
- 데이터 스키마 변경으로 dev 서버의 mock 저장소(globalThis 싱글톤)가 이전 스키마를 캐싱하고 있던 것을 발견해 dev 서버 재기동으로 반영
- `npm run lint`·`tsc --noEmit` 통과 확인, Playwright로 (1) 카드 클릭 시 상세 필드 정상 표시, (2) 패널 열린 상태에서 다른 카드 위치 클릭 시 백드롭이 가로채 카드 조작이 막히고 패널이 닫히는 것, (3) Esc, (4) X 버튼, (5) 빈 영역 클릭 총 4가지 닫기 경로를 각각 직접 검증

### 리뷰 / 검증
- 실제 화면에서 사이드패널 정상 작동 확인
- 사이드패널에 대한 요구 스팩은 없으나 현재 지원자의 상세 내역이라고 보기엔 데이터 정의가 미흡해 임의로 데이터 추가 결정

## [candidate-detail] 지원자 상세 컬럼 추가 (이력서/자기소개서/학력/경력)

### 프롬프트 1 (CLAUDE CODE)
> 지원자 상세에
> 이력서, 자기소개서, 학력, 경력, 같은 일반적인 컬럼들을 추가해줘

### AI 출력 요지
- `Candidate` 타입에 `education`(학력), `experience`(경력), `coverLetter`(자기소개서), `resumeUrl`(이력서 링크) 필드 추가, 시드 데이터 6건에 각각 값 채움
- `CandidateDetailPanel`에 학력/경력/자기소개서/이력서 항목을 연락처 다음, 메모 이전 순서로 추가. 이력서는 새 탭에서 열리는 링크(`이력서 보기 ↗`)로 표시 (실제 파일 서빙은 없으므로 example.com 목업 URL 사용)
- 데이터 스키마가 또 바뀌어 dev 서버의 mock 저장소(globalThis 싱글톤)가 이전 스키마를 캐싱하고 있던 것을 재기동으로 반영
- `npm run lint`·`tsc --noEmit` 통과 확인, Playwright로 패널 내 필드 라벨 순서(현재 단계/지원일/이메일/연락처/학력/경력/자기소개서/이력서/메모)와 이력서 링크의 href·target 속성을 직접 검증

### 리뷰 / 검증
- 데이터 반영 확인.
- 데이터에 비해 패널 너비가 부족하다 판단해 너비 확대 결정.

## [candidate-detail] 사이드 패널 너비 확대

### 프롬프트 1 (CLAUDE CODE)
> 사이드패널이 너무 작은데 화면 반 정도까지 나오게 조정해

### AI 출력 요지
- `CandidateDetailPanel`의 패널 너비를 `max-w-sm`(384px 고정)에서 `sm:w-1/2`(뷰포트 절반, 640px 미만에서는 `w-full`)로 변경
- Playwright로 1400px 뷰포트 기준 패널이 정확히 절반(700px)까지 넓어졌는지 스크린샷으로 직접 확인

### 리뷰 / 검증
- 실제 화면 정상 적용 확인
- 실제 코드상 책임 분리 의도대로 적용 확인

## [ui-states] 로딩/에러/빈 상태 UI 설계 논의

### 프롬프트 1 (CLAUDE CODE)
> 로딩/에러/빈 상태를 모두 UI로 처리할 필요가 있어
> 현재 프로젝트에서는 로딩이 반영될만한 곳이 상세보기 내용이 많은 경우 혹은 최초에 전체 지원자 목록을 로드하거나 할 때 정도 같은데
> 더 필요한 경우가 있는지 먼저 우선 파악해봐
> 코드 작성하지는 말고

### AI 출력 요지 (코드 변경 없음, 분석만 수행)
- 로딩: 카드 단계 이동(PATCH)은 이미 처리됨("저장 중…" + draggable 비활성화). 초기 목록 로드/상세 패널은 현재 동기 in-process 읽기라 실제 로딩 구간이 없어 지금은 해당 없음(추후 지연 시뮬레이션·API 분리 시 필요해질 항목)
- 에러: PATCH 실패는 이미 처리됨(롤백+토스트). 초기 목록/상세 로드는 위와 같은 이유로 현재는 실패 경로 자체가 없음
- 빈 상태: 컬럼 자체가 빈 경우는 이미 처리됨. **새로 발견한 실제 갭**: 필터(이름 검색/직무 체크박스)로 전체 컬럼이 비어도 지금은 컬럼마다 동일한 "지원자 카드가 없습니다"만 표시돼 원인 구분이 안 되고 필터 초기화 방법도 없음 — 이건 비동기와 무관하게 지금 바로 재현 가능한 갭
- 제안: 필터 빈 상태부터 먼저 처리, 초기 로드/상세 패널의 로딩·에러는 지연/실패 시뮬레이션을 실제로 붙이는 시점에 같이 설계

### 리뷰 / 검증
- 필터로 인한 빈 상태 갭 지적은 타당하다 판단.
- 다만 "현재는 동기 읽기라 로딩 구간이 없다"는 전제가, 요구사항에 이미 명시된 지연·실패 시뮬레이션을 붙이는 순간 깨질 것으로 보여 확인 위해 추가 질문.

### 프롬프트 2 (CLAUDE CODE)
> 이런 기술 논의 프롬프트도 전부 PROMPTS에 기록해
>
> 만약 현재 상태에서 mock API가 네트워크 지연(200~800ms)와 실패(약15%)를 의도적으로 작성되고, 1000건의 데이터를 msw 또는 json-server 같은 것으로 처리한다면 로딩이 들어갈 여지가 실질적으로 생기지 않아?

### AI 출력 요지 (코드 변경 없음, 분석만 수행)
- 결론: 그렇다. 프롬프트 1에서 "현재는 불필요"로 판단한 전제(동기·무지연·무실패 in-process 읽기)가 그 조건에서 깨지므로, 초기 목록 로드와 (향후 분리될 수 있는) 상세 로드 모두 로딩+에러 UI가 실질적으로 필요해짐
- 지연 200~800ms는 사람이 "즉시"로 인지하는 한계(~100ms)를 크게 초과 → Server Component에서 계속 fetch하면 `loading.tsx`(Suspense) 없이는 그 구간 동안 백지, 클라이언트 fetch로 바꾸면 보드 영역에 스켈레톤이 없으면 빈 공간이 뜸 → 두 경우 다 로딩 UI가 사실상 강제됨
- 실패율 15%는 이론상 케이스가 아니라 약 7회에 1회꼴로 실제 발생 → 초기 로드도 PATCH처럼 에러 상태 + 재시도 UI가 필요해짐
- MSW vs json-server는 "네트워크 계층에서 실제 요청/응답 사이클이 생긴다"는 점에서 로딩 필요성 판단에는 큰 차이가 없음(구현 위치 차이일 뿐, 핵심은 지연의 실재 여부)
- 1000건은 지연과 별개로 페이로드 파싱/초기 렌더 비용을 더해 체감 대기시간을 늘리며, 카드 약 1000개 동시 렌더 시 팝인이 보일 수 있음(단, 이는 가상화 논의에 가까운 별개 이슈로 지금 결론내지 않음)
- PROMPTS.md에 이번 논의(프롬프트 1, 2)를 [ui-states] 섹션으로 기록

### 리뷰 / 검증
- 전제가 깨진다는 답변 확인. 로딩/에러 UI를 별도로 먼저 만들기보다, 지연·실패 시뮬레이션을 실제로 적용한 뒤 함께 설계하는 순서로 결정.

## [network-sim] mock API 네트워크 지연/실패 시뮬레이션

### 프롬프트 1 (CLAUDE CODE)
> mock API에 해당 요구사항으로 작성해

### AI 출력 요지
- `src/lib/server/simulate-network.ts`: 200~800ms 랜덤 지연(`simulateNetworkDelay`)과 15% 확률 실패(`shouldSimulateFailure`)를 제공하는 공용 유틸 신규 작성
- 기존 `PATCH /api/candidates/[id]`에 지연+실패 시뮬레이션 적용 (검증 통과 후, 실제 저장 직전에 체크 — 실패 시 서버 상태는 변경되지 않음)
- 신규 `GET /api/candidates` 라우트 추가(동일한 지연+실패 시뮬레이션 적용), mock API로 읽기/쓰기를 모두 처리한다는 원래 요구사항에 맞춤
- `src/components/board/Board.tsx`: 초기 지원자 목록을 더 이상 서버에서 직접 주입받지 않고, mount 시 `GET /api/candidates`를 호출하도록 변경. 로딩 중에는 "지원자 목록을 불러오는 중…" 표시, 실패 시 에러 메시지 + "다시 시도" 버튼(재조회) 표시 — 다만 이 로딩/에러 UI는 최소 기능만 구현했고 디자인은 다듬지 않음(폴리싱은 별도 요청 시 진행 예정)
- `src/app/page.tsx`: 서버 사이드 store 직접 읽기(`getCandidates`, `force-dynamic`) 제거, Board가 자체적으로 데이터를 불러오도록 단순화
- `npm run lint`·`tsc --noEmit` 통과 확인. React 19 `react-hooks/set-state-in-effect` 린트 규칙 때문에 초기 구현(useEffect에서 setState를 동반한 async 함수를 직접 호출)이 실패해서, effect 본문은 프로미스 체인만 실행하고 재시도는 별도 상태(`reloadKey`)와 이벤트 핸들러에서 동기 setState를 하도록 재구성
- Playwright로 (1) 최초 진입 시 로딩 문구가 보였다가 보드가 렌더되는지, (2) GET을 강제로 500 응답하도록 가로채 에러+재시도 UI가 뜨는지와 재시도 성공 시 정상 렌더되는지, (3) curl로 30회 반복 요청해 지연 시간대(약 200~800ms)와 실패 응답이 실제로 섞여 나오는지 직접 확인

### 리뷰 / 검증
- 실제 페이지에서 최초 진입시 로딩 확인
- 간헐적 지연 및 실패 시뮬레이션 작동 확인
- 생성한 코드가 기존 책임분리 안에서 의도한대로 작성되었다 판단, 채택

## [candidate-fixtures] 1,000명 부하 테스트용 지원자 데이터

### 프롬프트 1
> 실제로 부하상태 테스트를 위해 지원자 데이터를 1000개 만들어줘

### AI 출력 요지
- `src/lib/candidates.ts`에 지원자 타입과 결정적 mock 데이터 생성 함수를 추가
- 기본 export로 단계별 데이터가 분산된 지원자 1,000개를 제공하고, 생성 개수는 인자로 조절 가능하게 구성

### 리뷰 / 검증
- 모듈 실행 검증 결과 1,000개 생성, ID 1,000개 고유, 5개 단계에 각 200개씩 분산, 상세 패널 필드 누락 없음
- 생성 함수 자체는 1,000개를 만들었지만, 실제 API 저장소는 기존 `globalThis` 상태를 개수 확인 없이 재사용하는 문제가 있었음
- 실행 중인 개발 서버에서 이전 6건 seed가 남아 API가 6건을 반환할 수 있다는 사실을 저장소 코드 정독과 실제 상태 확인으로 발견
- 초기 구현은 부분 채택하고, seed 개수 변경을 감지해 저장소를 재초기화하는 보완을 프롬프트 2에서 적용

### 프롬프트 2
> 켄디데이츠 참조에 문제가 있는 거 같은데. 레거시 목록이 출력되고 있어. 생성한 1000개 목록 출력되도록 수정해.

### AI 출력 요지
- 기존 생성 함수는 1,000개를 만들고 있었지만, `globalThis` mock 저장소가 이전 6건 seed를 재사용할 수 있는 문제를 확인
- 저장소의 현재 데이터 수가 최신 초기 데이터 수와 다르면 1,000건으로 재초기화하도록 수정

### 리뷰 / 검증
- 실제 페이지에서 지원자 1,000건 반환 확인
- 5개 단계에 각 200건씩 분산 확인
- 1000개 스크롤 부하 테스트를 위한 조치는 후순위로 보류

## [card-order] 컬럼 내 카드 순서(드롭 위치) 정확히 반영

### 프롬프트 1 (CLAUDE CODE)
> 코덱스로 지원자 1000개 작성해서 작동 확인까지 수행했어.
> 일단 지금 카드를 옮겼을때, 드래그한 위치에 정확히 끼워지질 않아서 동작 확인이 어려워서 옮긴 위치에 정렬되게 만들고 싶은데, 이거 적용하려면 일반 어레이 사이에 끼워넣으면 복사비용 이슈 있을 거 같은데 정렬 용 자료구조를 따로 만드는 편이 좋을까?
>
> 코드 작성하지말고 설계 논의만 진행해봐 일단

### AI 출력 요지 (코드 변경 없음, 설계 논의만 수행)
- 근본 원인: `Candidate`에 컬럼 내 순서 개념 자체가 없어서(stageId만 존재), 이동 시 항상 원래 배열 위치를 따라감 — 성능 문제가 아니라 누락된 기능
- "배열 복사 비용" 전제는 정정: 1000건 splice는 마이크로초 단위라 실제 병목 아님. 진짜 이유는 인덱스를 순서로 쓰면 카드 하나 이동 시 그 사이 카드 전부의 순서를 다시 계산/저장해야 한다는 점(현재 "이동한 카드 1건만 PATCH" 구조와 안 맞음)과 동시 이동 시 충돌 위험
- 제안: 카드마다 `order`(fractional index, Trello의 pos 방식과 동일) 필드를 스테이지별로 독립적으로 둠. 두 이웃 사이에 끼울 때는 중간값만 계산, 이동한 카드 1건만 갱신 → 기존 PATCH 구조 그대로 유지
- 대안(컬럼별 ID 배열을 별도로 관리)도 검토했으나, stageId와 별도 구조 두 곳에 순서 정보가 나뉘어 동기화가 깨질 위험이 있어 기각 — order 필드 하나로 진실의 원천을 유지하는 쪽이 안전하다고 판단
- 추가로 카드 단위 드롭 위치 감지(카드 상/하단 절반)가 함께 필요하다는 점도 명시

### 리뷰 / 검증
- 배열 복사 비용이 병목이라는 내 전제가 틀렸고, 순서 개념 자체가 누락된 문제였다는 지적 확인. fractional index 방향 채택.
- 다만 중간값을 계속 쪼개면 소수점이 무한히 깊어질 텐데 그 한계를 어떻게 처리하는지 확인 필요하다 판단.

### 프롬프트 2 (CLAUDE CODE)
> order = a + b / 2 방식으로 구현하면 float 소수점이 과도하게 깊어지는 경우 어떻게 대응하는데?

### AI 출력 요지 (코드 변경 없음, 설계 논의만 수행)
- 같은 두 이웃 사이에 반복해서 끼워 넣으면 이론상 약 50회 전후(더블 정밀도 상대오차 한계)로 중간값이 한쪽 값과 같아지는 시점이 반드시 옴을 설명 — 시작 간격을 넓게 잡아도 이 횟수 자체는 크게 달라지지 않음(상대적 감쇠라서)
- 대응: 중간값이 이웃값과 같아지는 "충돌"을 감지하면, 그 스테이지에 속한 카드만 현재 순서를 유지한 채 넓은 간격(1000, 2000, 3000…)으로 재정렬(rebalance) 후 재계산 — 드물게만 발생하고 해당 컬럼 크기만큼만 비용이 듦
- 더 견고한 대안(Jira LexoRank류 문자열 키, 이론적 정밀도 한계 없음)도 언급했으나 이 프로젝트 규모에는 과설계로 판단, float + 필요시 재정렬로 충분하다고 결론

### 리뷰 / 검증
- 충돌 시 해당 컬럼만 재정렬하면 된다는 대응 확인. LexoRank는 이 규모에 과설계라는 판단에 동의.
- 다만 장기간 사용하면 정밀도 소진 확률이 누적될 것으로 보여, 초기 로딩 시점 정규화를 함께 넣는 편이 낫다고 판단해 추가 지시.

### 프롬프트 3 (CLAUDE CODE)
> 해당 방향으로 설계안을 확정하지만 추가적인 조치를 취하자
> 한 번의 작업에서 같은 카드를 수십 수백번 조작하는 케이스는 별로 없겠지만
> 장기간 사용되는 경우 order 소수점의 문제가 발생할 확률이 더 늘어날 거 같은데
> order에 대한 정의를 이전값을 통해 초기 로딩시에 초기화하는 조치를 하면 실 사용감에 좋은 영향을 줄 거 같은데 이 부분 동의하면 그대로 진행해

### AI 출력 요지
- "초기 로딩"을 mock 저장소가 최초로 초기화되는 시점(서버 프로세스 부팅)으로 해석 — 그 시점에 각 스테이지별로 기존 순서("이전 값")를 유지한 채 깔끔한 간격(1000 단위)으로 order를 재배정. 이동 중 충돌 시 재정렬(프롬프트 2에서 합의)과 합쳐 이중 방어 구조
- API 계약 변경: PATCH body를 `{ stageId, beforeId, afterId }`로 바꿔 순서 계산/재정렬을 서버(store)가 전담하도록 함 — 클라이언트는 실수 정밀도 로직을 몰라도 됨
- `src/lib/candidates.ts`: `Candidate`에 `order` 필드 추가, `ORDER_GAP` 상수 export, mock 생성 시 스테이지별 순번으로 order 부여
- `src/lib/server/candidate-store.ts`: 부팅 시 `normalizeOrders`(스테이지별 재배정), 이동 시 `computeOrderBetween`(중간값 계산, 충돌 시 null), 충돌 시 `rebalanceStage` 후 재계산하는 `reorderCandidate`로 교체
- `src/app/api/candidates/[id]/route.ts`: `beforeId`/`afterId` 검증 추가, `reorderCandidate` 호출로 교체
- `src/components/board/Column.tsx`: children 대신 `candidates` 배열을 직접 받아 카드별 이전/다음 이웃을 계산해 `CandidateCard`에 전달하는 구조로 변경, 컬럼 빈 영역/마지막 카드 아래 드롭은 "맨 끝에 삽입"으로 처리
- `src/components/board/CandidateCard.tsx`: 카드 자체를 드롭 대상으로 만들어 마우스 Y좌표로 카드 상/하단 절반을 판정해 삽입 위치를 결정, 시각적 삽입선(위/아래 파란 테두리) 표시. 테스트 편의를 위해 `data-candidate-id` 속성 추가
- `src/components/board/Board.tsx`: `moveCandidate`가 `(candidateId, stageId, beforeId, afterId)`를 받아 로컬 배열을 splice로 낙관적 재배치(순서 필드는 서버 응답으로만 동기화), 실패 시 이동 전 스냅샷 전체로 롤백
- `npm run lint`·`tsc --noEmit` 통과 확인. Playwright로 1000건 데이터에서 특정 두 카드 사이로 정확히 드롭되는지, 새로고침 후에도 그 위치가 유지되는지 직접 검증. 별도로 같은 두 이웃 사이에 70회 연속 삽입하는 스트레스 스크립트로 정밀도 소진 → 자동 재정렬 → 이후에도 전체 순서 무결성 유지(중복/역전 없음)를 직접 확인.

### 리뷰 / 검증
- 수동 조작시 정상 작동 확인.
- 탭 인라인 스페이스가 2로 작성돼있던 것 4로 수정.
- 로직 및 책임분리상 문제는 없다 판단하여 채택.

### 프롬프트 4 (CLAUDE CODE)
> 같은 카드를 빠르게 연속 이동했을때 데이터가 깨지지 않도록 경쟁 상태가 안전하게 처리돼야해. 현재 구조에서 이 부분이 잘 적용돼있는지 점검해.

### AI 출력 요지 (코드 변경 없음, 점검만 수행)
- 점검 결과 안전하지 않음을 확인, 4가지 문제를 발견해 심각도순으로 보고:
  1. `moveCandidate` 실패 시 호출 "그 순간"의 전체 배열 스냅샷으로 롤백해서, 그 사이 동시에 성공한 다른 카드의 이동까지 되돌려버림 (15% 실패율 때문에 실사용에서 재현 잦음)
  2. `movingId`가 단일 값이라, 카드 A 이동 중 다른 카드 B를 이동시키면 movingId가 B로 바뀌면서 A가 응답 대기 중인데도 다시 드래그 가능해짐 → 같은 카드에 대해 PATCH 요청이 동시에 두 개 나갈 수 있는 유일한 단일 탭 경로
  3. 서버(`reorderCandidate`)에 낙관적 동시성 제어가 전혀 없어, 같은 카드에 대한 동시 요청은 "완료 순서"가 곧 "최종 상태"가 됨(요청 순서 아님) — 멀티탭 시나리오엔 무방비. API 계약을 건드리는 더 큰 설계 문제라 클라이언트 수정과 분리해서 제안
  4. 성공 응답 처리 시 `order`만 동기화하고 `stageId`는 그대로 둬서, 레이스 발생 시 두 필드가 다른 시점 값으로 섞일 수 있음
- 1, 2, 4번은 명확한 버그로 바로 수정 제안, 3번(서버 측 버전 체크 등)은 범위가 더 커서 별도 확인 요청

### 리뷰 / 검증
- 1,2,4번은 버그 원인으로서 작용할 가능성이 있을 수 있기에 우선 조치 결정.
- 3번은 실제로 사용시 어떤 문제인지 체감이 안되므로 일단 보류.

### 프롬프트 5 (CLAUDE CODE)
> 1,2,4번 먼저 조치해

### AI 출력 요지
- `src/lib/order.ts` 신규: `computeOrderBetween`을 서버 전용 위치에서 클라이언트/서버가 공유하는 위치로 추출 (Board의 낙관적 계산과 candidate-store의 실제 계산이 같은 로직을 쓰도록)
- `src/lib/server/candidate-store.ts`: 로컬에 중복 정의돼 있던 `computeOrderBetween` 제거, 공유 유틸 import로 교체
- `src/components/board/Board.tsx` 수정:
  - `movingId: string | null` → `movingIds: Set<string>`로 변경, 여러 카드가 동시에 "이동 중" 상태를 독립적으로 가질 수 있도록 함
  - `moveCandidate`가 더 이상 `candidates`를 클로저로 캡처하지 않고 전부 함수형 업데이트(`setCandidates(prev => ...)`)로 처리 — `useCallback` 의존성 배열도 빈 배열로 정리
  - 낙관적 반영/롤백/성공 동기화를 모두 "해당 candidateId 한 건"만 `prev.map`으로 갱신하도록 변경, 실패 시에도 그 카드가 이동 직전 가졌던 스냅샷(`snapshot`) 하나로만 되돌림
  - 낙관적 이동 시 배열 splice 대신 `computeOrderBetween`으로 임시 order를 계산해 필드만 갱신, 렌더링은 항상 `order` 기준 정렬로 위치를 결정 (splice 기반 위치 추적 자체를 제거해 로직 단순화)
  - 성공 응답은 서버가 반환한 지원자 레코드 전체로 교체(필드 일부가 아니라 통째로)
- `src/components/board/Column.tsx`: `movingIds` prop을 받아 카드별로 `movingIds.has(candidate.id)`로 `isMoving` 계산하도록 수정, 렌더링 목록도 항상 `order` 기준 정렬로 받음
- Playwright로 두 시나리오 직접 검증: (A) 카드 A 실패 + 카드 B 동시 성공 → A만 롤백되고 B의 성공은 보존되는지 (B) 카드 C 이동 중(2.5초 지연 mock) 다른 카드 D를 이동시켜도 C의 `draggable` 속성이 계속 `false`로 유지되는지 — 두 케이스 모두 통과 확인

### 리뷰 / 검증
- 현재 같은 카드를 연속으로 이동하는 것 자체가 원천적으로 불가능한 상태 -> 실 작동 검증 보류
- 구조 정독 -> 일단 수정 없이 현재 구조 반영 결정
- 카드 이동 후 다른 카드를 빠르게 이동시키면 그게 정상반영되지 않거나, 오류로 롤백됐을때 같이 롤백되는 현상을 확인
  - 해당 현상이 '낙관적 업데이트'와 '경쟁 상태 처리' 요구사항에 반하는 요소인지 판단 필요.
  - 해당 사안 검토, 요구사항에 대한 명백한 위반사항으로 보이진 않으나 결과가 바뀌는 듯한 사용감의 원인이 되는 지점. 낙관적 업데이트의 신뢰성 문제로 판단. 조치 결정.

### 프롬프트 6 (CLAUDE CODE)
> 3번 조치 시작해
>
> 그리고 1,2,3,4번 이슈가 뭐였는지 ### 비고 로 하나 만들어서 추가설명 기재해

### AI 출력 요지
- `src/components/board/Column.tsx`: `findStableNeighborId(candidates, movingIds, fromIndex, direction)` 헬퍼 추가 — 특정 방향으로 훑어 아직 이동 중이지 않은 첫 카드의 id를 반환. 컬럼 빈 영역 드롭(맨 끝 삽입)과 카드별 `onDropBefore`/`onDropAfter`의 반대편 이웃 조회 모두 이 헬퍼를 거치도록 변경해, `beforeId`/`afterId`가 이동 중인 카드를 절대 참조하지 않도록 함
- `src/components/board/CandidateCard.tsx`: 카드 자기 자신이 `isMoving`이면 dragover/dragleave/drop 핸들러가 `stopPropagation` 없이 그대로 반환 — 이벤트가 상위 Column으로 흘러가 "컬럼 끝에 삽입"으로 안전하게 대체 처리되도록 함 (이동 중인 카드는 다른 카드의 드롭 기준점으로 직접 잡히지 않음)
- 검증: 지난번 재현 스크립트(카드 A 응답 2초 지연 + 카드 B를 곧바로 A 뒤에 드롭)를 그대로 재실행 — 수정 전 A=601.56/B=11773.44로 엉뚱하게 벌어지던 값이, 수정 후 A=574.22/B=574.22로 **같은 값**이 됨. 완전히 해결된 건 아니지만 성격이 달라짐: 둘 다 "같은 안정된 이웃(마지막으로 확정된 카드) 뒤에 넣어줘"를 각자 독립적으로 요청해서 생기는 order 값 동률(tie)이라, 최종 화면 정렬은 두 카드가 여전히 인접해서 나오고(안정 정렬 특성상), 잘못된 컬럼/위치로 튀는 현상은 재현 안 됨. 남은 문제는 "값이 같아질 수 있다"는 것뿐이라 심각도가 크게 낮아졌다고 판단, 별도 조치 없이 일단 보고만 함

### 비고
경쟁 상태 점검에서 발견한 4가지 이슈 요약 (모두 [card-order] 범위):

1. **부분 롤백 문제**: `moveCandidate` 실패 시 호출 시점의 배열 전체 스냅샷으로 롤백해서, 그 사이 동시에 성공한 다른 카드의 이동까지 되돌려버림. 15% 실패율 때문에 실사용에서 재현 잦았음. → 프롬프트 5에서 수정(카드 1건 단위 롤백)
2. **movingId 단일값 문제**: `movingId`가 카드 하나만 기억해서, 카드 A 이동 중 다른 카드 B를 이동시키면 movingId가 B로 바뀌며 A가 응답 대기 중에도 재드래그 가능해짐 → 같은 카드에 PATCH가 동시에 두 번 나갈 수 있는 유일한 단일 탭 경로였음. → 프롬프트 5에서 수정(`movingIds`를 Set으로 변경)
3. **서버 동시성 조율 없음 (이번에 조치)**: 아직 자기 위치가 서버에 반영되지 않은(이동 중인) 카드를 다른 카드가 beforeId/afterId로 참조하면, 서버가 그 카드의 예전 순서값을 기준으로 계산해 엉뚱한 위치에 꽂히던 문제. → 프롬프트 6에서 "이동 중인 카드는 참조 기준점으로 쓰지 않는다"로 수정. 다만 "같은 안정된 이웃을 동시에 기준점 삼는" 경우 order 값이 같아지는 훨씬 약한 잔여 이슈는 남아있음(데이터 손상 없음, 화면상 인접 배치는 유지)
4. **부분 필드 동기화 문제**: 성공 응답 처리 시 `order` 필드만 동기화하고 `stageId`는 그대로 둬서, 레이스 발생 시 두 필드가 다른 시점 값으로 섞일 수 있었음. → 프롬프트 5에서 수정(서버 응답 레코드 전체로 교체)

### 리뷰 / 검증
- 실제 화면에서 반복 테스트상 정상 작동 확인.
- 추가적인 부하 테스트는 이후 진행하고 나머지 요구사항 우선 개발 진행 결정.

## [keyboard-nav] 키보드만으로 단계 이동 + 상세 열람

### 프롬프트 1 (CLAUDE CODE)
> 다음으로 키보드만으로 단계 이동과 상세 열람을 가능하도록 구성할거야
> 입력처리와, 실제 작동을 책임분리해서 구성하는 것으로 드래그&드롭이나 다른 입력과 호환 및 확장 가능한 형태로 구성하자
>
> 우선 방향키로 선택 대상 카드를 고를 수 있게 하고,
> q e 키를 통해 해당 카드를 보드상에서 이동할 수 있도록 구성해.
> 서류검토에서는 더이상 좌로 이동 불가능하고, 불합격에서는 더이상 우로 불가능하도록 예외처리 해두고
>
> 스페이스바 입력시 상세 정보 패널을 열도록 구성해
>
> 현재 선택한 카드가 뭔지 노란색 태두리로 시각화하고

### AI 출력 요지
- `src/lib/order.ts`: Column에 있던 `findStableNeighborId`를 이 공유 모듈로 이동 + export — 드래그앤드롭과 키보드 이동이 "컬럼 끝에 삽입" 위치 계산 로직을 동일하게 재사용하도록 함
- `src/hooks/useBoardKeyboardControls.ts` 신규: 입력 처리 전담 훅. 방향키로 `focusedCandidateId`(선택 카드)만 옮기고, q/e/스페이스는 `onMoveToStage`/`onOpenDetail` 콜백을 호출할 뿐 실제 이동·저장 방식은 전혀 모름 — 검색 인풋 등 타이핑 대상에 포커스가 있으면 모든 키를 무시
- `src/components/board/Board.tsx`: 기존 `moveCandidate`(드래그앤드롭이 쓰던 것과 동일한 액션 함수)를 그대로 두고, 키보드 전용 래퍼 `moveCandidateToStage(candidateId, stageId)`만 추가 — `findStableNeighborId`로 대상 컬럼 끝 위치를 계산해 동일한 `moveCandidate`를 호출. 즉 입력(키보드 vs 드래그)과 액션(카드 이동)이 완전히 분리되고 액션 함수는 하나만 존재
- 방향키: 상/하는 같은 컬럼 내 이전/다음 카드로, 좌/우는 인접 컬럼으로 포커스 이동(같은 인덱스 위치 우선, 없으면 clamp). q/e는 STAGES 배열 기준 이전/다음 스테이지로 이동, 서류검토에서 q·불합격에서 e는 범위 밖이라 조용히 무시. 이동 후에도 포커스는 id 기준으로 추적해 카드를 그대로 따라감
- `src/components/board/CandidateCard.tsx`: `isFocused` prop 추가, 선택된 카드에 `ring-2 ring-yellow-400` 시각 표시
- `npm run lint`·`tsc --noEmit` 통과 확인. Playwright로 첫 ArrowDown 시 서류검토 첫 카드 자동 선택, 상/하 이동, 좌/우 컬럼 전환, 서류검토에서 q 눌러도 컬럼 불변(경계 처리), e로 실제 단계 이동 + 포커스 유지, 스페이스로 상세 패널 오픈, 검색창 타이핑 시 q/e/스페이스가 board 액션으로 새지 않는지까지 7개 시나리오 모두 직접 검증

### 리뷰 / 검증
- 실 사용시 정상 작동은 확인했으나 이동한 카드가 최하단으로 이동하는 현상 확인. 사용감을 위해 같은 열 연산 추가 조치 필요 판단.
- 정독 결과 새로 생성된 코드가 인라인 2칸으로 적용된 형태 확인. 수정 조치 결정.

### 프롬프트 2 (CLAUDE CODE)
> 정상 작동은 확인했으나 이동한 카드가 최하단으로 이동하는 현상 확인. 사용감을 위해 같은 열 연산 추가 조치 필요할 것으로 보이는데
> 그리고 지금 코드들 왜 인라인 2칸으로 한거야? 4칸이 표준 아니야? 내가 일부러 4칸으로 고쳤는데 왜 다시 2칸으로 한거야. 4칸으로 고정해

### AI 출력 요지
- 인라인 2칸으로 새로 작성됐던 5개 파일(`Board.tsx`, `Column.tsx`, `CandidateCard.tsx`, `useBoardKeyboardControls.ts`, `order.ts`)을 전부 4칸으로 재변환(내용은 그대로, 들여쓰기만 2배로 확장). 프로젝트에 `.prettierrc`/`.editorconfig`가 없어 자동으로 강제되지 않는다는 점 확인, 재발 방지를 위해 이 사실을 메모리에 기록해둠
- q/e로 단계 이동 시 항상 컬럼 "끝"에 삽입되던 걸, 원래 컬럼에서의 상대 위치(인덱스)를 대상 컬럼 길이에 맞춰 클램프해 비슷한 자리에 넣도록 수정 — `useBoardKeyboardControls`의 q/e 핸들러가 `sourceIndex`(현재 카드의 컬럼 내 인덱스)를 `onMoveToStage`에 같이 넘기고, `Board.tsx`의 `moveCandidateToStage`가 그 인덱스를 대상 컬럼에서 `findStableNeighborId`로 앞/뒤 이웃을 찾아 beforeId/afterId를 계산하도록 변경
- `npm run lint`·`tsc --noEmit` 통과 확인, 모든 수정 파일이 4칸 들여쓰기인지 재확인. Playwright로 서류검토 4번째(인덱스 3) 카드를 포커스해 'e'로 면접(201건)으로 이동시켰을 때 최하단이 아니라 그대로 인덱스 3 위치에 꽂히는지 직접 검증

### 리뷰 / 검증
- 정상 작동 확인
- 단, 선택한 카드와 스크롤이 맞지 않는 현상 확인. 조치 필요 결정.

### 프롬프트 3 (CLAUDE CODE)
> 선택한 카드를 자기 컬럼 내에서 이동할때, 옆 컬럼으로 이동할때 모두 온전히 보이도록 스크롤을 쫓아오게 조치해

### AI 출력 요지
- `useBoardKeyboardControls`에 선택 카드의 위치(스테이지 인덱스/카드 인덱스)를 추적하는 `useEffect`를 추가해, 위치가 바뀔 때마다 `document.querySelector('[data-candidate-id=...]')`로 해당 카드 DOM을 찾아 `scrollIntoView({ block: "nearest", inline: "nearest", behavior: "smooth" })` 호출
- `focusedCandidateId`만 의존성으로 두면 q/e 이동(아이디는 그대로, 위치만 바뀜)을 놓치므로, `locate()`로 구한 `stageIndex`/`cardIndex`도 함께 의존성에 포함 — 컬럼 내 상하 이동, 컬럼 간 좌우 이동, q/e로 실제 단계가 바뀌는 경우 모두 이 하나의 effect로 커버됨
- `scrollIntoView`의 `block`/`inline` "nearest" 옵션 덕분에 컬럼의 세로 스크롤(overflow-y-auto)과 보드의 가로 스크롤(overflow-x-auto) 두 스크롤 컨테이너를 모두 필요한 만큼만 따로 처리 — 별도 좌표 계산 코드 없이 브라우저 내장 동작에 위임
- `npm run lint`·`tsc --noEmit` 통과 확인, 900×700 좁은 뷰포트로 Playwright 검증: (1) 같은 컬럼에서 ArrowDown 16회 연타 후 선택 카드가 화면 안에 완전히 들어오는지 (2) ArrowRight 4회로 불합격까지 이동 후 카드가 잘리지 않는지 (3) 'e'로 실제 단계를 이동시킨 뒤에도 이동한 카드가 완전히 보이는지 — 3가지 모두 좌표 기준으로 직접 확인

### 리뷰 / 검증
- 방향키 이동, 카드 이동, 상세 패널, 스크롤 정상 작동 확인
- 카드 이동을 키보드 입력으로 빠르게 작동했을 때, 지연 및 실패 타이밍에 따라 되돌아오는 지점을 예측하기 어려운 문제가 있음. 요구사항에 반하는 문제인지 추가 판단 필요. 우선 고려만 한 체로 기능 개발 진행.

## [undo] 카드 이동 Undo (스택 기반)

### 프롬프트 1 (CLAUDE CODE)
> 이어서 현재 입력 구조에 Undo를 적용할거야.
> 카드 이동을 스택 구조로 관리해서, 이전 이동 상태로 되돌릴거야.
> mock API의 에러 처리의 롤백과 꼬이지 않도록 구성해.
> undo는 컨트롤z와 보드 위에 Undo 버튼을 추가해서 작동하도록 해

### AI 출력 요지
- `Board.tsx`에 `MoveHistoryEntry { candidateId, previousStageId, previousOrder }` 스택(`moveHistory`) 추가. `moveCandidate`에 `recordHistory` 파라미터를 추가해 mock API 응답이 실제로 성공한 경우에만 스택에 push하도록 해서, 실패 시의 낙관적 업데이트 롤백 경로와 undo 스택이 서로 얽히지 않게 분리
- `handleUndo`: 스택 최상단을 pop하고, `order.ts`에 추가한 `findNeighborsForOrder`로 해당 카드가 원래 있던 order 값 근처의 현재 before/after 이웃을 다시 찾아 기존 `moveCandidate`를 `recordHistory=false`로 호출 — undo 자체가 새 히스토리 항목으로 다시 쌓이지 않도록 함. 이동 중인 카드가 스택 맨 위일 때는 `canUndo`가 false가 되도록 `movingIds`와 연동해 중복 undo를 방지
- `useBoardKeyboardControls.ts`: `onUndo` 콜백을 옵션으로 받고, 키다운 핸들러 최상단에서 Ctrl/Cmd+Z를 다른 방향키/q/e 처리보다 먼저 가로채 `onUndo()` 호출 — 기존 입력/액션 분리 구조를 그대로 유지
- 보드 우상단에 "실행 취소 (Ctrl+Z)" 버튼 추가, `canUndo`가 false면 disabled 처리
- `setCandidates` 업데이터 클로저 안에서만 대입되는 `let snapshot` 값을 이후 스코프에서 읽을 때 TS가 `never`로 좁혀버리는 문제가 2회 발생 — `snapshotStageId`/`snapshotOrder` 같은 원시값을 클로저 안 같은 지점에서 별도 `let`으로 뽑아내 이후 로직에서는 그 변수만 읽도록 수정해 해결
- `npm run lint`·`tsc --noEmit` 통과 확인. Turbopack `.next` 캐시로 인한 허위 useEffect 경고를 `rm -rf .next` 후 재확인해 실제 버그가 아님을 확인
- Playwright로 검증: 히스토리 비어있을 때 버튼 disabled, 버튼 클릭 undo, Ctrl+Z undo, 서로 다른 두 카드에 대한 LIFO 순서(가장 최근 이동부터 역순 복원, 서로 간섭 없음), 실패한 이동은 애초에 히스토리에 쌓이지 않는지(에러 롤백과 undo 스택 비간섭) — mock API의 실제 15% 랜덤 실패로 인해 첫 실행 결과가 비결정적이었던 것을 확인 후, PATCH를 강제 성공시키는 라우트 인터셉트로 재실행해 위 시나리오 모두 결정론적으로 통과 확인

### 리뷰 / 검증
- undo 버튼, 키보드 입력 정상 작동 확인
- 구현 코드 정독 과정에서 불필요하게 생성된 주석 제거 작업 병행

## [keyboard-nav] q/e 이동 방식 fix (숫자키 직행 + 서류검토-불합격 순환)

### 프롬프트 1 (CLAUDE CODE)
> fix 하나 진행하자. q e로 이동할때 클램프 없어도 될 거 같아. 서류에서 바로 불합격 보내는 동작이 필요할 수도 있으니까.
>
> (구체적으로 어떤 동작을 원하는지 확인 질문에 대한 답변) e/q를 전체 단계 자유 이동으로 변경

### AI 출력 요지
- q/e는 기존처럼 인접 단계 이동 + 경계 가드(서류검토에서 좌로/불합격에서 우로 불가)를 그대로 유지 — 그 방향엔 애초에 단계가 없다는 구조적 제약이라 유지하는 게 맞다고 판단
- 대신 숫자키 1~5(STAGES 순서와 매핑: 1=서류검토 … 5=불합격)를 추가해 어느 단계에 있든 원하는 단계로 한 번에 이동 가능하게 구현 — "서류검토에서 바로 불합격으로" 같은 요구를 q/e를 건드리지 않고 별도 메커니즘으로 충족
- `useBoardKeyboardControls.ts`: `STAGES.length` 기반으로 `STAGE_JUMP_KEYS` Set을 만들어 `HANDLED_KEYS`에 합침. keydown 핸들러가 숫자키를 감지하면 현재 단계와 같은 숫자면 무시(no-op), 다르면 기존 `onMoveToStage`(드래그앤드롭·q/e와 동일한 액션 함수)를 그대로 호출 — 입력(숫자키)과 액션(카드 이동) 분리 구조를 새 입력에도 그대로 적용
- `npm run lint`·`tsc --noEmit` 통과 확인. Playwright로 (1) 서류검토 카드에서 '5' 입력 시 즉시 불합격으로 이동 (2) 이미 불합격인 카드에서 '5' 재입력해도 안전하게 아무 변화 없는지 (3) '2'로 면접으로 재이동 (4) q/e가 기존처럼 인접 단계 이동으로 그대로 동작하는지 (5) 검색창 포커스 중 '5' 입력이 보드 액션으로 새지 않고 텍스트로 그대로 입력되는지 — 5가지 시나리오 모두 직접 검증

### 리뷰 / 검증
- 요청하지 않은 1~5번 키로 처리하는 기능이 추가됐지만, 사용성에 좋다 판단해 채택.
- 실제 화면상에서 요구사항대로 가장자리에서 넘어갈 수 있는 클램프 해제된 구조가 미적용되어있음. 추가 조치 필요.

### 프롬프트 2 (CLAUDE CODE)
> 요구하지 않은 사항 구현이어도 숫자키 1~5는 생각보다 편리해서 괜찮은데
> 왜 서류검토에서 q 누르면 불합격으로 넘어가고, 불합격에서 e 누르면 서류검토 넘어오도록 클램프 없는 구조는 수행 안 한거야?

### AI 출력 요지
- 직전 구현에서는 "서류검토 좌로/불합격 우로 불가"라는 최초 키보드 내비 요구사항의 경계 가드를 그대로 유지한 채 숫자키만 추가했는데, 이번 지시로 그 경계 자체를 순환 구조로 바꾸는 것으로 요구사항이 갱신됨
- `useBoardKeyboardControls.ts`의 q/e 케이스에서 경계 체크(`stageIndex > 0` / `stageIndex < STAGES.length - 1`)를 제거하고, `(stageIndex - 1 + STAGES.length) % STAGES.length` / `(stageIndex + 1) % STAGES.length`로 순환 이동하도록 변경 — 서류검토에서 q는 불합격으로, 불합격에서 e는 서류검토로 넘어감. 숫자키 1~5 직행 이동은 그대로 유지
- `npm run lint`·`tsc --noEmit` 통과 확인. Playwright로 (1) 서류검토에서 q → 불합격 순환 (2) 불합격에서 e → 서류검토 순환 (3) 중간 단계에서 e/q 일반 이동이 그대로 동작하는지 (4) 숫자키 직행 이동이 순환 로직과 함께 정상 동작하는지 4가지 시나리오 모두 직접 검증

### 리뷰 / 검증
- 실제 화면에서 정상 작동 확인.
- 드래그로는 '같은 카드를 빠르게 연속 이동했을 때 데이터가 깨지지 않아야함' 의 조건에 위배되는 동작을 확인하기 어려웠으나, 키 입력을 빠르게 하는 과정에서 의심 동작 확인. 추가적인 검토 진행.
  - 해당 동작이 정말로 데이터가 깨짐으로 판단해야하는 위배 사항인지, 확정 시점으로의 롤백 과정에서 입력 차이인지 판단하기 어려움.
  - 또한, 어떤 방향으로 수정해야 올바른 형태인지에 대한 방향성이 불분명한 상태.
  - 연속 동작에서 모두 서버 지연, 확률적 실패에 걸리는 것 자체가 사실 문제 상황일 수도 있다 판단, 연속 동작이 끝나고 500ms 동안 입력하지 않았을 때만 api 요청을 보내는 방식이 더 자연스러우며 불필요한 서버 요청을 줄일 수 있을 것으로 판단. 해당 방향의 타당성 검증 먼저 진행.

## [move-sync] 연속 이동 경쟁 상태 수정 (in-flight 가드 + 요청 디바운스)

### 프롬프트 1 (CLAUDE CODE)
> (위 [keyboard-nav] 리뷰/검증 내용을 인용하며)
> 현재 이 상태고,
> 스택에 저장되는 시점은 api 통신 성공 시점이니, 연속 이동 후 실패시 최초의 자리로 돌아오는 구조로로 만들려 하는데
>
> 타당성이나 리스크 검증해줘

### AI 출력 요지
- 코드 정독만으로 답하지 않고 Playwright로 PATCH 발신 횟수를 직접 계측한 결과, **기존 in-flight 가드가 전혀 동작하지 않고 있음을 확인**. 한 태스크에서 5연타 시 PATCH 5건(전부 동일 body), 40ms 간격 5연타 시 5건이 88/162/240/316ms 간격으로 전부 겹쳐서 발신됨
- 원인: `Board.tsx`가 `setMovingIds(updater)` 호출 **직후 같은 스코프에서** `alreadyMoving` 플래그를 되읽는 구조였는데, React는 updater를 호출 시점에 동기 실행해주지 않음. 특히 `useCandidateFilter`의 `useDeferredValue` 때문에 저우선순위 작업이 상시 pending이라 eager-state 최적화 경로도 막혀 있어, 연속 입력일수록 확실하게 실패하는 가드였음
- 따라서 리뷰에 적힌 "의심 동작"은 체감 문제가 아니라 실제 경쟁 상태가 맞다고 판단. 이전 턴에 내가 "가드가 동작하므로 데이터 오염 위험 없음"이라고 답했던 것을 계측 결과로 정정
- 제안하신 디바운스 방향은 타당하다고 판단(요구사항은 "모든 입력이 개별 요청이 될 것"이 아니라 "데이터가 깨지지 않을 것"이며, 5요청 시 최소 1회 실패 확률 `1-0.85⁵≈56%`가 1요청 시 15%로 감소). 단 **디바운스는 완화책이지 해결책이 아님** — 디바운스 후 발사된 요청의 비행 중에 다시 이동하면 겹침이 그대로 재현되므로 ref 기반 가드 수정과 병행해야 함을 지적
- "실패 시 최초 자리 복귀"는 타당할 뿐 아니라 디바운스를 제대로 구현하면 자동으로 따라오는 성질(버스트 1개 = 스냅샷 1개 = 요청 1개 = 히스토리 1개)이며, 아무것도 영속화되지 않은 상태에서 중간 위치를 보여주는 것이 오히려 거짓 표시이므로 더 정직한 동작이라고 판단
- 리스크로 (1) 디바운스 대기 중 새로고침 시 미저장 — 필수 요구사항 2번 위반 소지 (2) 카드별 키 분리 필요 (3) flush 시점 이웃 재계산 필요 (4) 대기 중 Undo 의미 모호를 제시

### 리뷰 / 검증
- 디바운스를 "불필요한 요청 절감" 목적으로 제안했는데, 계측 결과 기존 가드가 처음부터 동작하지 않았다는 답변 확인. 의심 동작이 체감 문제가 아니라 실제 경쟁 상태였음이 확정됨.
- 디바운스는 재현 빈도만 낮추는 완화책이라는 지적 납득. ref 가드 수정과 병행하는 방향으로 결정.

### 프롬프트 2 (CLAUDE CODE)
> 이 검증 대화는 프롬프트에 왜 작성 안 했어?
> 작성 후 1,2번 모두 진행해

### AI 출력 요지
- 기술 논의도 전부 기록하라는 기존 지시가 있었는데 "방향이 확정되지 않았다"고 임의 판단해 건너뛴 것이 잘못이었음을 인정하고, 검증 대화를 프롬프트 1로 소급 기록
- **1번(ref 가드)**: `movingIds` state를 되읽던 판정을 `moveStateRef`(Map)로 교체. ref는 동기적으로 읽고 쓸 수 있어 같은 태스크 안의 연속 호출도 정확히 막힘. 같은 원인의 버그를 막기 위해 `candidatesRef`도 도입해, 이동 계산은 항상 ref에서 읽고 `useState`는 렌더링용 사본으로만 유지
- **2번(디바운스)**: 낙관적 업데이트는 입력 즉시 반영하되 PATCH만 500ms 지연. 카드별 버스트 상태(`CardMoveState`)로 관리하며, 입력을 버리지 않고 비행이 끝나는 대로 최종 목적지만 이어서 보내는 drain 루프 구조라 카드당 요청이 절대 겹치지 않음
- 실패 롤백은 `confirmed`(서버가 마지막으로 확정해준 위치)로 되돌림 — 버스트 안에서 아무것도 확정되지 않았으면 그 자리가 곧 최초 자리이므로 요구하신 동작이 되고, 일부만 확정된 경우엔 원점 대신 확정 위치로 되돌려 서버와의 상태 불일치를 막음. 토스트 문구도 두 경우를 구분
- 새로고침 유실 리스크는 `pagehide` + `fetch(keepalive: true)`로 대기 중인 이동을 밀어 넣어 해소. 디바운스로 미뤄지는 동안 기준 이웃이 다른 단계로 가버릴 수 있어 `resolveIntent`로 발신 시점에 이웃 유효성을 재검증(둘 다 무효면 대상 단계 맨 뒤에 붙임)
- `npm run lint`·`tsc --noEmit` 통과. Playwright 계측으로 수정 전후 대조 검증: 한 태스크 5연타 PATCH 5건→**1건**, 40ms 5연타 5건(전부 겹침)→**1건**(버스트 중 발신 0건, 낙관적 UI는 즉시 이동), 비행 중 추가 이동 시 두 요청이 812ms 간격으로 **직렬화**됨을 확인
- 추가 검증: 버스트 도중 실패 시 중간 위치가 아닌 **출발 지점으로 복귀** + 문구 확인, 버스트 후 새로고침 시 최종 위치 유지, **디바운스 대기 중(80ms) 새로고침해도 keepalive flush로 유지**됨을 확인. 회귀 검증으로 undo 버튼/Ctrl+Z/두 카드 LIFO/실패 이동이 히스토리에 안 쌓임/드래그앤드롭 정상 동작 확인
- 부수 효과로 버스트 1개 = 히스토리 1개가 되어, 3연속 이동 후 Ctrl+Z 한 번이 버스트 전체를 되돌림(기존엔 이동마다 개별 항목)

### 리뷰 / 검증
- 실제 환경에서 연속 입력 안정성 확인.
- 현재 `Board.tsx`가 비대해지는 것을 확인, 책임 분리가 잘 되었는지 정독 및 추가 분석 예정.
- 500ms 이후 api 전송이 시작되는 시점에서 이동을 적용하면, api 전송이 확정되어 원 위치로 돌아오는 동작이 부자연스러움. 관련 조치 필요.

### 프롬프트 3 (CLAUDE CODE)
> 500ms 이후 mock API가 시작되는 시점에서 이동을 다시 시작하면, 내 이동에 대한 처리는 다시 시작됐는데, api가 실행되니까 결국 이전에 멈춘 시점을 기준으로 완결되기 때문에 마지막에 이동한 명령이 정상적으로 작동하지 않는 모습을 보여
> 이건 바로 코드 수정하지 말고 해결책을 한번 논의해봐야겠는데 추천 구조를 잡아봐

### AI 출력 요지
- 코드 수정 없이 먼저 Playwright로 재현 시도. 단순 2연속 이동(같은 카드, 두 번째 이동을 요청 비행 중에 실행)은 정상 동작했으나, 3연속 이동 체인에서 재현됨 — 두 번째·세 번째 요청이 완전히 동일한 목적지("offer")로 중복 발송되어, 세 번째 이동이 실제로는 아무 진전도 없이 씹힌 것과 같은 결과가 나옴
- 원인: `flushMove`의 `while(state.pending)` 루프가 응답을 받을 때마다 `state.pending` 상태를 확인하지 않고 무조건 `writeCandidates`로 화면을 덮어씀. 이미 낡은(먼저 보낸) 요청의 응답이 늦게 도착하면, 그사이 사용자가 이미 만들어둔 더 최신 낙관적 상태를 순간적으로 되돌려버리고, 그 찰나에 새 입력이 들어오면 그 되돌아간 지점 기준으로 다음 목적지를 계산해 진행이 막힘
- 이전에 Codex가 지적했던 "오래된 응답이 최신 상태를 덮는다"는 주장과 동일한 메커니즘임을 확인 — 그때는 새로고침/이탈 타이밍으로만 검증하다 재현 실패로 판단했으나, 실제로는 페이지가 열려있는 정상 상태에서 세 번째 조작이 그 짧은 창에 걸릴 때 나타나는 것이었음
- 추천 구조로 "화면 갱신은 응답이 진짜 마지막(그 시점에 pending이 비어있음)일 때만 하고, 장부(`state.confirmed`)는 매번 갱신" 하는 게이트를 제안. 구조를 새로 만들 필요 없이 기존 `flushMove` 응답 처리 부분에 조건 하나만 추가하면 됨을 코드 라인과 함께 제시

### 리뷰 / 검증
- 2연속에서는 재현되지 않고 3연속부터 재현된다는 점 확인.
- 제안된 조건부 게이트가 최선인지 판단 전에, UI를 아예 서버 응답과 분리하는 쪽이 더 단순한 구조가 아닌지 의문이 생겨 추가 검증 요청.

### 프롬프트 4 (CLAUDE CODE)
> 혹은, 저장은 해놓돼 갱신만을 막는 형태는 어때?
> 서버 응답이랑 상관없이 UI는 독립적으로 낙관적 적용으로 수행하고
> 500ms 시점에서 재입력 유예 시간을 500ms 다시 정의해서 그 시간안에 추가 입력이 없을때 확정하는 형태로
> 연속 입력인 경우에는 api 응답이 성공인 경우에 큐에 저장하고, 실패면 큐에 저장 안하고
> 이걸 확정될 때까지 큐에 저장하다가, 최종에 확정된 사항만 undo 스택에 추가하는 구조로
>
> 유효한지, 약점 없는지 검증해봐

### AI 출력 요지
- 제안을 3개 요소로 분해해 각각 검증: (1) "UI는 서버 응답과 무관하게 독립적으로 낙관적 적용" — 타당하고 이전 제안(조건부 게이트)보다 더 근본적. 단, 문자 그대로 "영원히 무관"하면 서버 쪽 리밸런스(order 재계산)를 클라이언트가 영영 못 따라잡는 드리프트 위험이 있어, "체인이 최종 확정되는 순간엔 서버 값으로 동기화"로 좁히는 걸 권고
- (2) "성공시 큐에 저장, 실패시 저장 안 함" / "확정된 것만 undo 스택에" — 이미 지금 `state.confirmed`(실패 시 갱신 안 됨)와 `state.recordHistory`(루프가 끝까지 성공했을 때만 push)가 정확히 이 동작을 하고 있음을 확인. 새로 만들 필요 없음
- (3) "500ms 재입력 유예를 재정의" — 타당한 아이디어지만 별도의 최적화이지 이번 버그의 원인이 아님을 구분해 설명. 비행 중이던 요청이 끝나자마자 큐에 있던 다음 의도를 즉시 보내는 대신 다시 500ms를 기다리게 하는 변경인데, (1)만으로 버그는 이미 다 해결됨. 지연 증가, `flushMove`의 while 루프를 타이머 기반 구조로 바꿔야 하는 구현 복잡도, 연속 조정 중엔 아무것도 저장 안 되는 특성을 약점으로 제시

### 리뷰 / 검증
- 제안한 3가지 중 2가지(성공 시에만 큐 저장, 확정분만 undo 스택)는 이미 구현돼어 있는 상태와 같은 것을 확인.
- 3번(재입력 유예 재정의)은 의도와 다르게 해석된 것으로 판단해 보류.
- 낙관적 업데이트는 최초부터 요구했던 사항인데 왜 지금 어긋나는지가 더 근본적인 의문이라 판단, 원인 재확인 필요.

### 프롬프트 5 (CLAUDE CODE)
> 3번은 내가 의도한거랑 다른 의미로 해석한 거 같으니까 우선 보류하고.
> 1번, 초기에 낙관적 UI 적용을 초기에 명령했을텐데 이게 현재 그럼 올바르게 낙관적으로 작동하지 않다는 의미로 보이는데 현재
> 왜 그렇게 된 건지를 문제 부분을 파악하고 설명 및 코드라인을 제시해줘

### AI 출력 요지
- 낙관적 업데이트 계약("화면은 항상 최신 사용자 의도를 보여주고, 실패했을 때만 되돌린다")을 지키는 지점(`requestMove`의 즉시 낙관적 쓰기, Board.tsx:287-293)과 어기는 지점(`flushMove`의 무조건적 응답 반영, Board.tsx:196-205)을 정확히 대조해 제시
- 두 지점 모두 결국 같은 `writeCandidates`를 호출하는데, 이 함수 자체엔 시간 순서 개념이 없어서 화면에 남는 게 "최신 사용자 의도"가 아니라 "가장 나중에 도착한 비동기 작업"이 되어버림을 설명. 재현 시나리오를 라인 단위로 대입해 왜 세 번째 입력이 잘못된 목적지를 계산하는지 구체적으로 증명

### 리뷰 / 검증
- 낙관적 업데이트 자체가 빠진 게 아니라, 응답 처리 쪽에 "이 응답이 아직 유효한가" 검증이 없어서 스스로를 덮어쓰는 경로가 하나 더 생긴 것임을 확인. 코드 상에 반복문에서 해당 부분 확인.
- 그렇다면 요청을 큐로 관리하는 형태가 맞는 방향인지 확인이 필요하다 판단.

### 프롬프트 6 (CLAUDE CODE)
> 그럼 요청 작업 큐를 만들고, 실패시 마지막 지점으로 되돌리면서 큐를 비우는 형태로 가고
> 성공시, 다음 큐... 아니 이 경우에는 스택이나 그냥 마지막 요청을 실행해보면 되는 건가
> 근데 그러면 현재 구조랑 크게 다를바 없지 않나?

### AI 출력 요지
- 사용자가 설명한 구조(요청 큐, 성공 시 다음 것 실행, 실패 시 큐 비우고 마지막 확정 지점으로 롤백)가 이미 `state.pending`(단일 슬롯 큐)·`while(state.pending)` 루프·`state.confirmed`·`moveStateRef.current.delete`로 정확히 구현돼 있음을 코드 라인과 함께 확인해줌 — "다를 바 없다"는 판단이 맞다고 확인
- 버그는 요청/큐 계층이 아니라 "화면에 그리는 시점" 단 한 곳(Board.tsx:196-205)에만 있고, 필요한 수정은 새 구조가 아니라 그 지점에 `if (!state.pending)` 조건 하나 추가하는 것뿐임을 명확히 함

### 리뷰 / 검증
- 제안한 큐 구조가 이미 이전과 다를바가 없음. 문제는 큐 관리 계층이 아니라 화면 반영 시점 한 곳뿐이라는 점 확인.
- 다만 실패했을 때 남은 큐를 이어서 시도하는 동작이 사용감상 맞는지 의문이 생겨 추가 검토 요청.

### 프롬프트 7 (CLAUDE CODE)
> 남은 큐를 계속 시도할 필요가 애초에 있는가가 의문인데
> 하나가 실패해서 되돌아왔는데, 자동으로 남은 큐를 실행하거나 최신 큐를 자기가 실행해버리면 오히려 의도하지 않은 지점으로 이동되는 듯한 사용감을 줄 거 같은데 검토 좀 해봐

### AI 출력 요지
- 현재 `catch` 블록은 실패 시 while 루프를 끝까지 안 돌고 즉시 빠져나와 전체를 롤백하므로, "실패한 요청만 빼고 남은 큐를 이어서 시도"하는 로직은 이미 없음을 재확인
- "이어서 시도"를 자동으로 넣는 안을 검토한 결과, 사용자 입력 없이 카드가 한 번 더 움직여 보이는 점, 실패한 요청 이후에 계산된 의도가 여전히 유효한지 보장 안 되는 점, 에러 토스트 문구가 그 이후 상태를 설명 못 하게 되는 점을 근거로 확장하지 않는 게 맞다고 판단. 실패 시엔 화면도 함께 확정 지점으로 되돌리고 있어 "화면엔 남아있는데 서버엔 없는" 불일치도 없음을 재확인, 사용자가 에러를 보고 직접 재시도하는 현재 흐름을 그대로 유지하기로 결론

### 리뷰 / 검증
- 실패 후 자동 재시도의 경우는 사용자 입력 없이 카드가 한 번 더 움직이는 동작은 예측이 어려우므로 현행 유지로 결정.
- 논의로 설계가 확정됐다고 판단해 구현 진행 지시.

### 프롬프트 8 (CLAUDE CODE)
> 지금 논의에서 결론난 설계가 맞는 거 같아.
> 일단 이대로 진행하고, 기술논의 프롬프트 또 작성 안됐던데 기재한 뒤에 진행해

### AI 출력 요지
- 이번 논의(프롬프트 3~8)를 소급 기록. 실제 코드 변경은 논의 결과대로 `flushMove`의 응답 처리 지점(Board.tsx:196~) 딱 한 곳: 응답이 오면 `state.confirmed`는 항상 갱신하되, 그 시점에 `state.pending`이 비어있을 때(=이 응답이 진짜 마지막일 때)만 `writeCandidates`로 화면에 반영하도록 `if (!state.pending)` 게이트 추가. 실패 처리(전체 롤백 후 중단)는 논의대로 그대로 유지, 손대지 않음
- 버그를 처음 재현했던 3연속 이동 체인 시나리오(각 입력을 이전 요청이 비행 중인 시점에 맞춰 실행)를 수정 전/후로 대조 검증: 수정 전엔 2번째·3번째 요청이 완전히 동일한 목적지로 중복 발송되어 최종 위치가 +2에서 멈췄는데, 수정 후엔 3회 모두 정확히 순차적인 별개 요청으로 나가 최종 위치가 항상 +3(최종합격)으로 정확히 도달함을 3라운드 반복으로 확인, 새로고침 후에도 유지됨을 재확인
- 회귀 검증: undo 버튼/Ctrl+Z/버스트 단위 undo/두 카드 LIFO/실패 이동이 히스토리에 안 쌓임/드래그앤드롭/디바운스 대기 중 새로고침 유지까지 전부 재확인, 이상 없음

### 리뷰 / 검증
- 3연속 이동 체인에서 최종 위치가 의도대로 반영되는 것 확인. 수정 전 재현되던 중복 목적지 발송이 사라짐.
- undo, 드래그앤드롭, 새로고침 유지 등 기존 동작 이상 없음 확인.

## [virtual-scroll] 1,000건 렌더링 성능 측정 및 가상 스크롤 필요성 검토

### 프롬프트 1 (CLAUDE CODE)
> 이제 1000건 성능 쪽 조치하자.
> 지금 카드가 전부 DOM에 그려지고 메모이제이션도 없는 상태인데, 실제로 얼마나 느린지 측정부터 하고 진행해.
> 그 근거로 memo만으로 충분한지 가상화까지 필요한지 판단해서 알려줘.
> 간단하게 해결되는 것부터 적용하고, 측정 수치는 문서에도 남겨

### AI 출력 요지
- 측정 방법: dev 서버는 React 개발 모드 오버헤드로 수치가 왜곡되므로 **프로덕션 빌드**(`next start`)로 측정. 카드 이동은 낙관적 업데이트가 keydown 시점에 동기 적용되므로 네트워크와 무관하게 렌더 비용만 분리 측정 가능. "빠르다"는 결론이 측정 실패 때문일 가능성을 배제하기 위해 **MutationObserver로 실제 DOM 변경(카드가 컬럼을 옮김, 30건 mutation)을 확인한 시점까지**를 측정하고, 저사양 기기 조건은 CDP CPU 스로틀링 6배로 재현. GET/PATCH의 15% 랜덤 실패는 라우트 인터셉트로 제거해 측정 노이즈 차단
- **측정 결과 스크롤은 이미 문제가 없음**: 1,000장 전량 렌더(DOM 노드 6,084개) 상태에서도 스크롤 60fps, long task 0건이며 6배 스로틀에서도 59.7fps·long task 0건 유지. 스크롤은 재렌더가 아니라 이미 그려진 DOM의 컴포지팅이라 카드 수의 영향을 거의 받지 않음 → **가상화는 지금 문제를 해결하지 않는 조치라고 판단해 도입하지 않음**
- **실제 비용은 카드 이동 시 재렌더였음**: memo가 없어 카드 한 장을 옮길 때마다 1,000장 전체 컴포넌트 함수가 재실행됨. 6배 스로틀에서 keydown→DOM 반영 68.2ms(최대 94.9ms)로, 저사양 기기에서 체감될 수 있는 구간
- 조치: `CandidateCard`를 `memo`로 감쌈. 다만 `Column`이 카드마다 인라인 화살표 함수로 `onDropBefore`/`onDropAfter`를 내려주고 있어 그대로는 매 렌더 prop이 바뀌어 memo가 무효화됨 → 카드가 자기 id와 삽입 위치("before"/"after")만 돌려주는 `onDropRelative` 단일 콜백으로 인터페이스를 바꾸고, 이웃 계산에 필요한 최신 `candidates`/`movingIds`는 ref로 참조해 `useCallback([])`으로 **렌더 간 동일한 함수 참조**를 유지하도록 변경. `candidate` 객체는 이동한 카드만 새 객체로 교체되고 나머지는 참조가 유지되므로 기본 얕은 비교로 충분
- 렌더 중 ref 쓰기를 금지하는 `react-hooks/refs` 린트 규칙에 걸려, ref 갱신을 커밋 후 effect로 옮김(드롭은 렌더 종료 후 발생하므로 최신값 보장)
- 측정 결과(프로덕션 빌드, keydown→DOM 반영, 7회 중앙값): **6배 스로틀 68.2ms → 37ms (약 46% 감소)**, **스로틀 없음 7.7ms → 4.4ms (약 43% 감소)**. 스크롤 fps와 long task는 변화 없음(원래 문제가 아니었음)
- 회귀 검증: memo 적용 후 포커스 링이 안 뜨는 현상이 있었으나, 원인은 이전 프로덕션 서버 프로세스가 살아있는 채로 `.next`를 다시 빌드해 청크 매니페스트가 어긋난 것(EADDRINUSE로 재시작 실패)이었고 코드 문제가 아님을 확인. 서버를 정상 재기동 후 정상 동작
- 드롭 콜백 인터페이스를 바꿨으므로 정밀 위치 드롭을 별도 검증: 특정 카드 **위쪽 절반에 드롭 → 그 카드 바로 앞**, **아래쪽 절반에 드롭 → 그 카드 바로 뒤**에 정확히 삽입되고 새로고침 후에도 위치가 유지됨을 확인. undo/Ctrl+Z/버스트 단위 undo/두 카드 LIFO/실패 이동 히스토리 미기록/컬럼 드롭까지 기존 회귀 항목 전부 통과. `lint`·`tsc`·`build` 통과

### 리뷰 / 검증
- 최초 렌더링 원활 확인
- 실제 스크롤시 체감 지연 없으며, 해당 클로드가 보고한 수치 콘솔로그 확인 후 로그 코드 제거.
- 카드 이동시 렌더 시간 50ms 내외 확인
- 검색어 필터, 직군 필터 적용시 지워서 전체 렌더링 시에는 200ms, 필터된 대상의 경우 9~50ms 로 수치 양호하다 판단.
- 요구사항 충족했다 판단, 채용. 단 현재 전체적으로 코드 가독성이 안좋다 판단해 다음 작업에서 리팩토링 진행 예정.

## [board-refactor] Board 책임 분리 (데이터 로딩 / 카드 이동 엔진 / 조합)

### 프롬프트 1 (CLAUDE CODE)
> Board가 467줄까지 커져서 책임분리 진행하자.
> 지금 한 파일에 목록 로딩, 카드 이동 엔진, undo 스택, 필터 조합, 렌더링이 전부 섞여있는데 훅으로 쪼개서 Board는 조합만 하도록 정리해.
> 특히 이동이랑 undo는 이번에 경쟁 상태 버그가 계속 나왔던 부분이니까 UI랑 완전히 떼어놔야 나중에 테스트 붙이기 편할 거 같고.
> 동작은 그대로 유지하는 게 전제니까 기존 회귀 검증 다시 돌려서 확인하고

### AI 출력 요지
- Board의 책임을 5가지(목록 로딩 / 카드 이동 엔진 / undo / 필터·그룹핑 조합 / 렌더링)로 정리한 뒤, 훅 2개로 분리
- `src/hooks/useCandidates.ts` 신규: 목록 조회·보관만 담당(`candidates`, `candidatesRef`, `writeCandidates`, `isLoading`, `loadError`, `retryLoad`). ref와 state를 함께 맞추는 `writeCandidates`도 여기로 이동
- `src/hooks/useCardMoves.ts` 신규: 낙관적 반영·디바운스 전송·drain 루프·실패 롤백·pagehide flush·undo를 전부 담당하고 UI는 전혀 모름. 이번 세션에서 버그가 집중됐던 로직이 한 파일에 모여, Board를 렌더링하지 않고도 이 부분만 테스트할 수 있는 형태가 됨
- undo는 이동 엔진과 순환 의존(이동이 히스토리를 쌓고, undo가 다시 이동을 호출) 관계라 별도 훅으로 빼지 않고 `useCardMoves` 안에 함께 둠. 대신 이웃 계산에 필요한 `candidatesByStage`를 인자로 받아, 기존의 "필터된 뷰 기준으로 위치 계산" 동작을 그대로 보존(더 정확해 보이는 전체 목록 기준으로 바꾸는 것은 동작 변경이라 리팩토링 범위에서 제외)
- 키보드 전용 진입점 `moveCandidateToStage`도 `candidatesByStage`에 의존하므로 같은 훅으로 이동
- `candidatesByStage`(필터 결과 + 스테이지 그룹핑)는 Board가 조합의 주체이므로 Board에 남김. 상세 패널 선택 상태도 3줄이라 훅으로 빼지 않음
- 결과: Board.tsx 467줄 → 143줄, `useCandidates.ts` 64줄, `useCardMoves.ts` 337줄
- `lint`·`tsc --noEmit`·`build` 통과. 동작 보존 확인을 위해 기존 검증 3종 재실행: 회귀 6종(undo 버튼/Ctrl+Z/버스트 단위 undo/두 카드 LIFO/실패 이동 히스토리 미기록/드래그앤드롭) 전부 통과, 정밀 드롭(카드 상/하단 절반 → 바로 앞/뒤 삽입 + 새로고침 유지) 통과, 오래된 응답이 최신 상태를 덮던 3연속 이동 체인도 3라운드 모두 +3단계로 정확히 도달

### 리뷰 / 검증
- useCandidate, useCardMoves는 의도대로 잘 분리 확인
- 로딩도 보드가 보유해야할 책임이라고 보기엔 상위 컨테이너로서의 책임으로 판단. 추가 분리 결정.

### 프롬프트 2 (CLAUDE CODE)
> 보드는 최상위 컨테이너로 치고, 로딩과 로딩에러를 처리하는 보드 상태와 실제 데이터가 렌더링되는 보드 뷰로 나눠서 추가 분리 진행해

### AI 출력 요지
- 지적에 동의 — 로딩·에러는 데이터 수명주기 관심사라 보드가 알 필요가 없다고 판단하고, Board를 "데이터 게이트"와 "실제 보드"로 한 단계 더 분리
- `Board.tsx`: `useCandidates()`만 호출해 로딩/에러/준비완료 중 무엇을 보여줄지 결정하는 33줄짜리 얇은 진입점으로 축소. `page.tsx`가 쓰는 이름이라 그대로 유지해 외부 영향 없음
- `BoardView.tsx` 신규: 목록이 준비된 뒤의 보드 화면. `candidates`/`candidatesRef`/`writeCandidates`를 props로 받고 로딩·에러 상태는 아예 모름 → fetch를 흉내내지 않고 순수 데이터만으로 렌더링 테스트가 가능한 형태
- `BoardStatus.tsx` 신규: 로딩과 에러가 "가운데 정렬된 메시지"라는 같은 모양이라 하나로 합침. 재시도 핸들러가 있으면 버튼도 함께 렌더링. 원본의 두 상태가 텍스트 색이 미묘하게 달랐어서(zinc-400 vs zinc-500) 그 차이까지 그대로 보존
- 결과: Board 33줄 + BoardView 121줄 + BoardStatus 28줄 (직전 단계의 143줄에서 다시 분리)
- `lint`·`tsc --noEmit`·`build` 통과, 회귀 6종 재통과. 로딩·에러 마크업이 새 파일로 옮겨갔으므로 GET 응답을 지연/500으로 강제하는 테스트를 따로 만들어 (1) 로딩 문구 표시 (2) 에러 문구 + 재시도 버튼 표시 (3) 재시도 클릭 시 실제로 1,000건 복구까지 직접 확인

### 리뷰 / 검증
- Board, BoardStatus, BoardView -> 의도와 동일한 구조로 분리 확인. 채택.


## [keyboard-nav] 카드 포커스·스크린리더 접근성 보완

### 프롬프트 1 (CLAUDE CODE)
> 접근성 마저 처리하자.
> 지금 노란 테두리로 선택 표시는 되는데 그게 실제 브라우저 포커스랑 따로 노는 거 같은데, Tab으로는 카드에 아예 접근이 안 되고 스크린리더도 뭐가 선택됐는지 못 읽을 거 같거든.
> 상세 패널도 열었을 때 Tab이 뒤쪽 보드로 새는지 확인하고, 닫으면 원래 카드로 포커스 돌아오게 해.
> 카드 이동 성공했을 때도 화면으로만 알 수 있고 소리로는 아무것도 안 나오는 것도 같이 봐줘.
> 기존 단축키 동작이랑 충돌 안 나게 하는 게 제일 중요하고, 끝나고 회귀 검증 돌려

### AI 출력 요지
- 로빙 탭인덱스 도입: 보드 전체에서 카드 1장만 `tabIndex=0`, 나머지는 `-1`. Tab으로 보드 진입 후 방향키로 이동
- 카드에 `role="option"`·`aria-selected`·`aria-label`(이름/직무/단계), 컬럼 목록에 `role="listbox"` 부여
- 방향키 이동 시 `focus()`를 함께 호출하고 카드 `onFocus`로 선택 상태를 되돌려 받아, 커스텀 선택 표시와 DOM 포커스를 일치시킴
- 상세 패널에 Tab 순환 트랩 추가(기존에는 실제로 뒤쪽 보드로 빠져나갔음). 닫힌 뒤 포커스 복귀는 패널과 훅이 경합하지 않도록 훅 한 곳에서만 처리
- Enter로도 상세 열기 추가. 카드에 개별 `onKeyDown`을 붙이면 전역 핸들러와 이중 발화하므로 입력 해석은 기존대로 `useBoardKeyboardControls`에 유지
- 이동 성공을 `aria-live="polite"`(sr-only)로 안내. 실패는 기존 `ErrorToast`의 `role="alert"`가 담당
- 미사용 export `MOCK_CANDIDATES` 제거
- `lint`·`tsc`·`build` 통과. Playwright 9종 검증(탭인덱스 1개, Tab 도달, `aria-selected` 1개, 포커스 동행, Enter 열기, 트랩 유지, Esc 후 복귀, live region 문구, 검색창 키 미유출) + 기존 회귀 6종·정밀 드롭 재통과

### 리뷰 / 검증
- tab키 포커스 정상 작동 확인.
- 방향키 이동 정상 작동 확인.
- 스페이스바, 엔터로 상세 패널 작동 확인.
- q e 로 카드 이동 및 컨트롤z로 undo 작동 확인

